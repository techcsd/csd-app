# HANDOFF — CSD App

## 🟢 SESIÓN 17/08 (2) — PROMPT-18 FASE 2/3/5 (AS2-9,15,16,18-20,25) — **RELEASE 1.79.0 PUBLICADO (rolling)**
**SHIPPED:** commit **`74eb12c`** → push `main` → PWA por Vercel. **APK 1.79.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, 8 cambios curados) + subido al bucket + `publicada=true` 1.79.0 (1.78.0→false). **3 migraciones aditivas a prod:** `2026-08-17-as6-confirmaciones-chofer.sql`, `2026-08-17-as2-notificar-despachante.sql`, `2026-08-17-publicar-1.79.0.sql`. **Rollback:** `update sgc.app_versiones set publicada=(version='1.78.0') where plataforma='movil';` + `git revert 74eb12c && git push`; backend: `drop trigger trg_conduce_notificar_despachante on sgc.salidas_inventario;` + restaurar `mis_confirmaciones` a la versión previa (solo receptor).

**Descubrimiento clave:** casi todo el backend de FASE 2 YA estaba desplegado (PROMPT-17): `conduce_firmar_despachante`, `mis_conduces_por_firmar(_count)`, `conduce_firma_despachante_pendiente`, `conduce_marcar_entregado` con gate **DR456** (no entregar sin firma), `conduce_detalle_app` con `despachante`+`motivo_label`, y el trigger `tg_conduce_origen_distinto_destino`. El trabajo fue **cablear el cliente**.

- **AS2 (firma remota del despachante — anti-fraude) ⚠️ CAMBIO DE FLUJO:** se **quitó el pad del despachante** del wizard del chofer (`generar-conduce`): ahora solo se exige la firma del CHOFER; el despachante (si es usuario del sistema) firma desde SU teléfono. Trigger nuevo `trg_conduce_notificar_despachante` → aviso "Conduce por firmar" con deep-link a `conduce-detalle`, donde el despachante ve un bloque "Firmar conduce" (signature-pad → sube a bucket `conduces` → `conduce_firmar_despachante`). El server ya bloquea la ENTREGA (DR456) hasta la firma. **⚠️ OPERATIVO:** esto activa el requisito de firma remota para TODOS los conduces con despachante del sistema — brief a los despachantes. Reversible (restaurar el pad + `firmasOk` con ambas firmas). Despachante de texto libre (no-usuario) NO requiere firma.
- **AS3:** "Entregado por" = `despachante` real (antes leía `entregado_por_nombre`=null); "Motivo" usa `motivo_label` del server con fallback local (`uso_proyecto`→"Uso en proyecto").
- **AS4:** bloqueo origen=destino en el wizard (computed `mismoOrigenDestino` por id + nombre → `destinoOk` false + banda de error).
- **AS5:** fotos de evidencia + firmas clickables → lightbox (patrón reportar-vehiculo) en `conduce-detalle`.
- **AS6:** `mis_confirmaciones` ampliado (aditivo) para que el chofer vea las confirmaciones de sus entregas confirmadas (`recibido_en not null` + entregado_por/conductor = yo).
- **AS7/AS8:** login ya NO bloquea a usuarios sin módulos (`afterAuth`): entra con aviso informativo y usa Notas/Tareas/Mensajes/Dudas/Reportar (rutas ya `authGuard+pinGuard`). El tile "Por confirmar" del home ya no cuela por `porConfirmar()>0` (la banner accionable lo cubre).
- **AS9:** toggle mostrar/ocultar contraseña en el login (SVG ojo/ojo-tachado).
- **AS15:** "Marcar N como atendidos" en Avisos de flota (`atenderAvisos` bulk `.in()`, ConfirmDialog, respeta filtro, excluye bloqueos).
- **AS16:** empty state de Registrar combustible con botón "🚗 Usar un vehículo" → `/transporte/uso-vehiculo`.
- **AS18:** "ya reportado por" resuelve el nombre vía `usuarios_por_ids` (fallback en `ReporteSemanalService`) + UI "otro usuario" si sigue vacío.
- **AS19:** `combustible-log` responsive (`.clog__filtros` flex-wrap + campos min-width + "Aplicar" a fila completa <380px).
- **AS20:** card "En uso" (transporte hub) sin botón "Uso de vehículo" (reaparece en "Para usar" al soltar).
- **AS25:** nueva pantalla `/mensajes/nuevo-grupo` (foto+nombre+descripción+integrantes con buscador) reusando `crear_grupo`→`grupo_editar`→`grupo_set_avatar`; la opción "Grupo" del ＋ ahora navega allí.

### 🟢 AS17 (FASE 5) — Uso de vehículo con 4 fotos — **RELEASE 1.80.0 PUBLICADO (rolling)**
Migración `sql/2026-08-17-as17-uso-vehiculo-fotos.sql` (aditiva): 4 columnas `foto_*_path` en `vehiculo_usos` + RPC `set_uso_fotos(uso_id, frente, izq, der, trasera)` (gate: dueño/flota/admin). Cliente: `uso-vehiculo` muestra 4 photo-slots (solo cámara) al tomar/recibir (no al soltar); tras `iniciar_uso_vehiculo` sube las fotos al bucket `vehiculos` y las adjunta con `set_uso_fotos` (best-effort). **Commit + APK + publicada=1.80.0** (1.79.0→false). **Follow-up:** mostrar esas fotos en los HISTORIALES de uso (AK18) — hoy se guardan pero la vista de historial aún no las pinta. **Rollback:** `update sgc.app_versiones set publicada=(version='1.79.0') where plataforma='movil';` + `git revert` del commit de AS17; backend: `drop function sgc.set_uso_fotos(uuid,text,text,text,text);` (las columnas son inocuas).

### 🟢 FASE 4 inventario (parcial) — **RELEASE 1.81.0 PUBLICADO (rolling)**
**SHIPPED** (commit abajo) + APK 1.81.0 firmado/registrado/subido + `publicada=1.81.0` (1.80.0→false). **1 migración de publicación** (`2026-08-17-publicar-1.81.0.sql`). **Descubrimiento:** `bodegas` YA tenía `proyecto_id/latitud/longitud/direccion_geo/ubicacion_hereda_proyecto/ubicacion_metodo`, y existían `set_apertura`, `set_apertura_lote`, `registrar_conteo_app`, `apertura_efectiva` — FASE 4 fue casi todo cliente.
- **AS12 (ubicación de almacenes):** editor en `almacenes` con 2 modos — **Vincular a obra** (`CollapsibleSelect` de `directorio_proyectos` → `proyecto_id` + `ubicacion_hereda_proyecto` + hereda lat/lng) o **Ubicación propia** (`LocationPicker` → lat/lng/direccion + `ubicacion_metodo`). Modelo `BodegaAdmin`/`BodegaUbicacion` + `crear/actualizarBodega` extendidos.
- **AS11 (editar stock):** `almacen-inventario` con botón "✏️ Contar / ajustar stock" (gateado por `puedeOperarSubmodulo('inventario.conteos')`) → `/inventario/conteo?bodega=<id>`; `ConteoPage` preselecciona por `?bodega=`.
- **AS13 (conteos+ajustes unificados):** `ConteoPage` renombrada "Conteos y ajustes" + botón "🕘 Historial"; `ConteosPage` "🔢 Nuevo conteo o ajuste"; ambas enlazadas (un submódulo).
- **⚠️ DEFERIDO de AS11:** "agregar artículo NUEVO del catálogo" al almacén — necesita la **decisión apertura-vs-entrada** (el prompt la marca) + article-picker en el conteo + que `registrar_conteo_app` cree la fila de un artículo ausente. **AS10** (herramienta admin de apertura): backend listo (`set_apertura_lote(bodega, cantidad, solo_faltantes, ids, incluir_todo_catalogo)`); propuesta = **solo web**; en app queda consulta. **⏸ seed masivo 1,000 sigue en pausa** (tu OK del alcance).

### 🟢 FASE 6 (parcial) — AS14 gastos directos — **RELEASE 1.82.0 PUBLICADO (rolling)**
**SHIPPED** + APK 1.82.0 + `publicada=1.82.0` (1.81.0→false). **Descubrimiento:** el backend de AS14 YA estaba desplegado (PROMPT-17, seed de hoy 15:15): tabla `gastos_directos` + `gasto_categorias` (8 categorías) + RPCs `registrar_gasto_directo`, `gastos_directos_de_proyecto`, `puede_registrar_gasto_directo`, `gasto_real_proyecto`. **Cliente:** en `compras-proyecto` se añadió pestaña **"Gastos directos"** (además de "Compras" formales) — lista `gastos_directos_de_proyecto` con total, y **form "➕ Registrar gasto"** (categoría del catálogo, concepto, monto, fecha, **foto del recibo** al bucket `obra`) gateado por `puede_registrar_gasto_directo`. `ProyectosService` con `getGastoCategorias/gastosDirectos/puedeRegistrarGasto/registrarGastoDirecto/reciboUrl`. **Respondida la pregunta de Xaviel:** el historial de "Compras del proyecto" sale del RPC `compras_de_proyecto` (órdenes de compra + ferretería); los gastos directos son un flujo NUEVO, separado y etiquetado.
- **AS22 (proveedores con ubicación): N/A en la APP.** El CRUD de proveedores es **solo-web (SGC)** — la app no tiene form de proveedor (solo consume `ferreterias_visibles`). `proveedores.lat/lng` YA existe en el esquema → **AS22 es tarea de la web SGC** (aplicar el input de ubicación estándar en el form de proveedor, normal + ferretería).

### 🟢 AS24 — QA del módulo Proyectos — **RELEASE 1.83.0 PUBLICADO (rolling)**
**SHIPPED** + APK 1.83.0 + `publicada=1.83.0` (1.82.0→false). Pase de QA con 2 agentes de revisión + fixes. **11 arreglos aplicados:** detalle muestra **presupuesto + fecha_fin_real**; **accesos directos** del detalle a "Compras y gastos" (`?obra=` preselecciona) y "Personal de obra"; variantes CSS de estado `planificacion`/fase `pendiente`; form de editar que no cargó → error + volver (antes: form en blanco mudo); validación de fechas (fin≥inicio) + presupuesto≥0; **cronograma**: línea "Hoy" alineada con las barras (offset de la columna de nombre 40%), estado vacío cuando hay tareas sin fechas de plan, error de carga ya no se ve como "sin cronograma" (toast), filtro de tareas `es_prueba`, embed del nombre de proyecto en avisos normalizado (array→objeto), deep-link a tarea inexistente avisa.
- **⚠️ HALLAZGOS DOCUMENTADOS (no arreglados — más grandes / decisión):** (form) no se puede asignar **responsable** al crear (falta user-picker); crear/editar es **online-only** (no outbox); posible **proyecto sin ubicación** si `set_proyecto_ubicacion` falla tras crear (crear-con-coords en una llamada); código `PROY-####` generado en cliente (race → debería ser RPC server-side). (cronograma) **AS21-relevante:** el modelo de consulta (`cronograma_tareas`/`listar_cronograma`) **NO** tiene volumetría / avance real % / avance esperado % / rendimiento / responsable — el import .mpp/.xlsx requiere **extender ese modelo + RPC + UI** primero; `puede_gestionar_cronograma` es online-only (offline no deja gestionar aunque las escrituras van por outbox); sin eje de fechas ni agrupación por fase (Gantt). Nits: a11y de filas del timeline, `severidad` sin usar, tipo de aviso crudo si no mapeado.

### 🟢 AS21 — Importar cronograma desde Excel (.xlsx) — **RELEASE 1.84.0 PUBLICADO (rolling)**
**SHIPPED** + APK 1.84.0 + `publicada=1.84.0` (1.83.0→false). **Descubrimiento:** el backend YA estaba desplegado (PROMPT-17): `cronograma_tareas` tiene `responsable/volumetria/avance_pct/rendimiento/grupo/import_lote/import_origen`, existe el RPC **`cronograma_importar(proyecto, fase_nombre, tareas jsonb, reemplazar)`**, y `listar_cronograma` ya devuelve esos campos vía `to_jsonb(t)`. **AS21 = solo cliente.**
- **Parser .xlsx en el cliente** (`core/util/cronograma-xlsx.ts`, dep nueva **`fflate`** para descomprimir): detecta la fila de headers (con "ACTIVIDADES"), mapea columnas por texto (# | ACTIVIDADES | RESPONSABLE | VOLUMETRÍA | FECHA INICIO | FECHA FIN | DÍAS | STATUS | AVANCE REAL % | AVANCE ESPERADO % | RENDIMIENTO), convierte fechas serial de Excel → ISO, detecta filas de sección (grupo, ej. "ENTREPISO #"), deriva fin = inicio+días si falta, normaliza %. **Validado contra el archivo real** `CRONOFRAMA MONTEREZZO TORRE 2.xlsx` (35 actividades, grupo, fechas correctas).
- **Página de import** `/proyectos/:id/cronograma/importar` (`CronogramaImportarPage`): file picker → preview (torre/etapa=hoja, #actividades, secciones, advertencias, tabla de las primeras 30) → toggle "reemplazar" → confirmar → `cronograma_importar`. Botones "📥 Importar" en el header del cronograma y en su empty-state (gateados por `puedeGestionar`).
- **Cadena validada end-to-end contra prod** (impersonando admin en una tx, luego limpieza): parser → `actividadToTareaRpc` → `cronograma_importar` → filas insertadas con el mapeo correcto (nombre/responsable/volumetria/fechas/días/avance/grupo). Datos de prueba borrados.
- **Consulta:** el detalle de cada actividad ahora muestra sección/responsable/volumetría/avance/rendimiento (los campos ya venían en `listar_cronograma`; solo faltaba el modelo TS + UI).
- **⚠️ .mpp DEFERIDO:** MS Project binario — no se parsea en el cliente. La app pide **exportar a Excel** (mensaje claro al elegir un .mpp). Opción futura: parseo server-side con mpxj vía edge function (SGC).

### 🟢 AS23 — Filtro de proyectos por zona — **RELEASE 1.85.0 PUBLICADO (rolling)**
**SHIPPED** + APK 1.85.0 + `publicada=1.85.0` (1.84.0→false). Xaviel definió "por ubicación" = **dropdown de zonas** (Cap Cana, Punta Cana, Santo Domingo, etc.). Client-only: `zonaDeProyecto()` (en `proyecto.model.ts`) deriva la zona del texto de `ubicacion`+`direccion_geo`+`localidad` contra `ZONAS_PROYECTO` (lista curada de zonas RD con keywords); el listado añade un `CollapsibleSelect` "📍 Zona" (solo si hay >1 zona) que filtra. **Verificado:** los 11 proyectos reales clasifican 100% (Cap Cana 3, Santo Domingo 3, Cana Bay 2, San Pedro/Punta Cana/Vista Cana 1 c/u; 0 sin clasificar).

### ⚠️ PENDIENTE de PROMPT-18 — colas menores / otro repo (no bloquean)
- **AS22** (proveedores con ubicación) → **web SGC** (la app no tiene form de proveedores; `proveedores.lat/lng` ya existe).
- **AS10 / AS11-agregar-artículo** (inventario): AS10 apertura admin (propuesta solo-web; `set_apertura_lote` listo) + AS11 "agregar artículo NUEVO del catálogo" al almacén (decisión apertura-vs-entrada).
- **AS21 .mpp** (parser MS Project binario — mpxj vía edge function, SGC). La app ya pide exportar a Excel.
- **Follow-ups documentados de QA (AS24):** form de proyecto (asignar responsable, offline/outbox, proyecto sin ubicación si falla set_ubicacion, código PROY server-side), cronograma (permiso offline, Gantt axis/fases, a11y timeline), AS17 (pintar las 4 fotos de uso en historiales).

### ✅ ESTADO PROMPT-18: app-side COMPLETO (7 releases: 1.78 → 1.85)
FASE 1 (tracking, falta validación en vivo con Misael), FASE 2 (conduces + firma despachante), FASE 3 (sin-rol + login), FASE 4 (inventario: ubicación/ajuste/conteos; colas AS10/AS11-agregar), FASE 5 (flota + uso con fotos), FASE 6 (gastos, cronograma import xlsx, proyectos QA, zonas). Pendientes = físicos de Xaviel (validación tracking, briefing despachantes, device-QA) + web SGC (AS22) + colas de decisión.
- **AS22 (web):** input de ubicación en el form de proveedores (SGC).
- **FASE 4 restante:** AS11-agregar-artículo nuevo (decisión apertura-vs-entrada) + AS10 apertura admin (propuesta solo-web).
- **AS17 follow-up:** pintar las 4 fotos de uso en los historiales.

## 🟢 SESIÓN 17/08 — PROMPT-18 FASE 1 (AS1) — 🔴 TRACKING desacoplado de rutas — **RELEASE 1.78.0 PUBLICADO (rolling) · falta SOLO la validación en vivo con Misael**
**SHIPPED:** commit **`73794be`** → push `main` → **PWA por Vercel**. **APK 1.78.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, 3 cambios curados) + subido al bucket (`csd-app-1.78.0.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` 1.78.0** (1.77.1→false) → `version_publicada()`=**1.78.0**; `version_minima` sigue **1.70.0**. **4 migraciones aplicadas a prod:** `2026-08-17-as1-tracking-continuo.sql`, `2026-08-17-as1b-chofer-breadcrumb.sql`, `2026-08-17-publicar-1.78.0.sql` (+ el flip). **Rollback:** `update sgc.app_versiones set publicada=(version='1.77.1') where plataforma='movil';` + `git revert 73794be && git push`; para el backend: `update sgc.parametros set valor='100' where clave='gps_precision_max_m';` + `drop function sgc.mi_config_tracking(); drop function sgc.otros_rastreados(); drop function sgc.chofer_breadcrumb_vivo(uuid,integer);` + `delete from sgc.usuario_flags where flag='comparte_ubicacion' and usuario_id in (select id from sgc.usuarios where nombre ilike '%misael%');`.

**Diagnóstico con evidencia de prod (eslabón por eslabón) → causa raíz encontrada:** el tracking solo capturaba DENTRO de una "ruta activa" formal. Evidencia: `chofer_ultima_posicion` mostraba posiciones de **días** (POLIN 15h, FELIX 2d, Misael 3d, Papo 6d) y **el 100% de las filas de `gps_ingesta_log` traían `ruta_id`** → cero captura sin ruta. Eso explica TODAS las quejas de Misael ("dura días", "3-4 días para moverse", "activos en el mismo lugar"). Segundo hallazgo de evidencia: el server **descartaba** la mayoría de los puntos capturados (`recibidos:40→insertados:1`, varios lotes `insertados:0`) por el filtro de precisión (100 m, muy estricto para gama baja en camión). El "hace 144h" de Papo NO es bug de cálculo: es su última posición real de hace 6 días. El "histórico de Papo vacío" tampoco es bug: sus 13 rutas son `es_prueba=true` y `rutas_historial` las oculta a no-admin (Papo es chofer de prueba). La alarma del domingo YA es autónoma (AlarmScheduler/WeeklyAlarm/BootReceiver).

**El backend YA estaba listo para tracking continuo** (contradice el handoff de PROMPT-10): `sgc.comparte_ubicacion()` existe, `registrar_posiciones` acepta puntos sin ruta y gatea por esa flag, `chofer_ultima_posicion` ya está en la publicación realtime, y el rol `chofer_transportista` tiene `comparte_ubicacion=true`. Faltaba que **el cliente capturara en continuo** (no solo en ruta) + tuning de ingesta.

**Migraciones aplicadas a prod (aditivas, verificadas) — falta espejar a SGC:**
- `sql/2026-08-17-as1-tracking-continuo.sql`: (a) `gps_precision_max_m` 100→**200** (dato, reversible; evidencia de descarte masivo); nuevos params `gps_distance_filter_m=25`, `gps_flush_seg=45`, `tracking_activo_min=15`; (b) **`mi_config_tracking()`** RPC — el cliente pregunta si DEBE rastrear (por `comparte_ubicacion`) y con qué cadencia; (c) **`otros_rastreados()`** RPC — cierra AO6: Seguimiento incluye a quien comparte ubicación sin ser chofer (Misael), gate flota-elevado, forma igual a `choferes_estado`; (d) `usuario_flags` override `comparte_ubicacion=true` para **Misael Encarnacion** (reversible con delete).
- `sql/2026-08-17-as1b-chofer-breadcrumb.sql`: **`chofer_breadcrumb_vivo(usuario_id, minutos=180)`** — trazo EN VIVO por chofer independiente de ruta (la "línea que sigue las calles" del tracking continuo; map-matching = v2).

**Cliente (Angular) — `npm run build` exit 0:**
- `TrackingService`: **modo continuo (AS1)** — `evaluarModoContinuo()` llama `mi_config_tracking()`; si comparte, arranca el tracking TODO EL TURNO (no atado a ruta). Cuando hay ruta activa, etiqueta los puntos con `ruta_id`; al terminar la ruta **NO se detiene** (solo quita la etiqueta). **Latido de frescura (heartbeat 150s)**: fuerza un fix aunque el chofer esté parado (distanceFilter no dispara sin movimiento) → "hace X"/mapa nunca congelados. distanceFilter/flush configurables desde el server. `apagar()` en logout. Notif del FGS re-copy ("ubicación activa", no "ruta activa").
- Hooks en `app.ts`: arranca en el primer NavigationEnd fuera de `/auth` (una vez por login) + en `resume` (nativo). `SessionService.logout()` → `tracking.apagar()`.
- **Batería REAL (AS1)**: `AppSettingsPlugin` (nativo) + `PermissionsService` — `isIgnoringBattery()` / `requestIgnoreBattery()` disparan el diálogo nativo `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` (antes era solo un toast de instrucciones). El aviso de batería ahora tiene botón "Permitir" y no molesta si ya está excluida.
- `Seguimiento`: une `otros_rastreados()` (Misael aparece en el mapa) + **trazo por chofer** (`chofer_breadcrumb_vivo`) en vez de solo por ruta.

**⚠️ PENDIENTE:**
1. **🔴 VALIDACIÓN EN VIVO OBLIGATORIA con Misael/jefe de flota** (física, tuya — la tanda AS1 NO cierra sin esto): un chofer real con la 1.78.0 instalada, abre la app, concede ubicación "todo el tiempo" + acepta la exclusión de batería → Misael/tú ven su posición **< 2 min** siempre (aunque cierre la app / apague pantalla) + **el trazo dibujándose** en Seguimiento. Documentar su OK explícito. **Ojo:** cada chofer debe ABRIR la 1.78.0 al menos una vez para que arranque el tracking continuo (y conceder los permisos).
2. **Espejar a SGC** (repo padre): las 2 migraciones de contrato (`as1-tracking-continuo`, `as1b-chofer-breadcrumb`) + revisar si la **web** de Seguimiento debe incluir `otros_rastreados`/`chofer_breadcrumb_vivo` (Hard Rule #5). Los 3 SQL ya están en este repo.
3. **Verificar en device:** (a) la alarma del domingo suena con la app CERRADA (ya es autónoma; solo confirmar permiso de alarma exacta en Android 14); (b) que Misael (no chofer, flag puesta) aparezca en el mapa al abrir su app; (c) batería: el diálogo nativo real aparece y, al excluir, el reporte sigue con pantalla apagada.
4. **Residual honesto**: si el SO MATA el proceso (swipe-away / OEM agresivo), el re-arranque depende de reabrir la app (boot/resume re-evalúan). El heartbeat+watchdog cubren estancamientos EN-app; la exclusión de batería evita la mayoría de kills de OEM. Un re-arranque headless real necesitaría un servicio nativo propio (fuera del alcance del plugin community) — documentado, no oculto.
5. **FASE 2–6 de PROMPT-18** (conduces/firma despachante, sin-rol, inventario, flota, proyectos/cronograma, grupos): NO tocadas — dependen de PROMPT-17 (SGC, sin commitear). Próxima(s) sesión(es).

## 🟢 SESIÓN 15/08 — PROMPT-16 ronda AR (app) — Registro de Personal EN OBRA por hojas + consulta — **RELEASE 1.77.1 PUBLICADO (rolling)** · device-QA ✅
Consume **PROMPT-15 (SGC AR, commits `f4b461a` + `353b1e8`)** — verificado por introspección en prod: tablas `sgc.cargos` (14 cargos seed), `personal_obra`, `personal_obra_fotos` (check tipo: persona|documento|pared|carnet|persona_carnet_cedula), `personal_obra_firmas`; RPCs `emitir_carnet_personal(p_id)` (número CSD-###### atómico, idempotente) + `personal_obra_conteos(p_proyecto_id)`; helpers obra-scoped `puede_ver_/gestionar_personal_obra`; bucket privado `personal-obra` (15 MB); submódulo `proyectos.personal` en rol admin. **`npm run build` exit 0** (qrcode a chunk lazy; warning de budget inicial 865 kB es preexistente, bajo el error 1.5 MB). **SHIPPED:** commit **`a65a6df`** → push `main` → **PWA por Vercel**. **APK 1.77.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, 5 cambios curados) + subido al bucket (`csd-app-1.77.0.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` 1.77.0** (1.76.0→false) → `version_publicada()`=**1.77.0**; `version_minima` sigue **1.70.0**. **2 migraciones aplicadas a prod:** `sql/2026-08-15-ar1c-…` (obra-scoped storage, aditiva — ver abajo) y `sql/2026-08-15-publicar-1.77.0.sql` (flip). AR1c **mirroreada al repo SGC** (commit `6108546`, push `main`). **Rollback:** `update sgc.app_versiones set publicada=(version='1.76.0') where plataforma='movil';` + `git revert a65a6df && git push`; políticas: reaplicar las 3 role-only de la migración AR1 + `drop policy "personal-obra: actualizar" on storage.objects;`.

- **FASE 1 — Registro EN OBRA por hojas (AR1-app):** wizard offline-first `PersonalRegistroPage` (`/proyectos/personal/registrar`), una pregunta por pantalla: **Datos** (obra —preseleccionada con `ctx.obraActiva()` o `?obra=`—, nombre, apellido, nacionalidad con `option-button` 🇩🇴/🇭🇹/🌎, cargo del catálogo con `collapsible-select`, teléfono) → **Documento** (tipo cédula/pasaporte/carnet electoral/ninguno + número + foto solo-cámara) → **Fotos guiadas** (persona*, pared, carnet, persona+carnet+cédula; cada `photo-slot [gallery]=false` con su guía visual + hint) → **⏸ Firma** (paso PREPARADO tras `FIRMA_HABILITADA=false`: al ponerlo en true aparece el paso y el servicio ya sube la firma por outbox, sin rehacer nada) → **Carnet** (`PersonalCarnet` con QR generado offline desde el id → apunta al expediente web `sgcconstructorasd.com/proyectos/personal/:id`; número "pendiente" hasta sincronizar) → **Resumen → Registrar**. Borrador AE9 en todo el wizard: texto por `autosave.queue`, **fotos persistidas a `borrador_fotos` al capturarlas** (sobreviven al kill del SO) → retoma con banner. Envío = **UNA op de outbox `personal_registro`** (client UUID = id de `personal_obra`, idempotencia).
- **FASE 2 — Consulta (AR1-app):** `PersonalListaPage` (`/proyectos/personal`): conteos rápidos arriba (total + por nacionalidad + por cargo, **client-side** sobre lo visible/filtrado, espejo de la web), buscador (nombre/cédula/cargo/carnet), filtros (obra, cargo, nacionalidad, estado) en panel, `LiveRefreshDirective` (pull-to-refresh + foreground), FAB "＋" gateado. Row → `PersonalExpedientePage` (`/proyectos/personal/:id`): datos, **carnet con QR**, **galería de las 5 fotos con lightbox** (overlay propio; no hay componente lightbox en la app), **editar datos inline** (`personal_editar` por outbox) + **activar/desactivar** (`ConfirmDialog`), todo gateado por permiso (`puedeGestionar`).
- **Arquitectura:** `core/models/personal-obra.model.ts` (espeja SGC), `core/services/personal-obra.service.ts` (getCargos/getObras cache-then-network, listar/getById/getFotos/getFirmas/fotoUrl firmada, `enqueueRegistro`/`enqueueEditar`/`enqueueEstado`; handlers `personal_registro` [upsert personal por id → upsert 5 fotos → firma ⏸ una-vez → `emitir_carnet_personal`] y `personal_editar`; `throwSyncError`), `shared/ui/personal-carnet/*` (carnet + QR `qrcode@1.5.4`, logo texto "CSD" —la app no tiene logo imagen—), páginas en `pages/proyectos/personal/`. **Booteado en `app.config.ts`** (S30) para que capturas offline sincronicen tras reinicio en frío. Rutas añadidas ANTES de `proyectos/:id` con solo `[authGuard, pinGuard]` (obra-scoped por RLS, como por-confirmar/confirmaciones; tile/FAB gatean en cliente). **Home tile** "🧑‍🔧 Personal de obra" gateado amplio (admin/proyectos/rrhh/dirección/`puedeVerSubmodulo('proyectos.personal')`/`puedeVerObra()` para capataz/ingeniero).

### 🔴 Migración AR1c APLICADA a prod (aditiva, retrocompat) — hueco de contrato de PROMPT-15
`sql/2026-08-15-ar1c-personal-obra-storage-obra-scoped.sql`. Las políticas del bucket `personal-obra` de AR1 eran **solo por módulo** (admin/proyectos/rrhh/direccion/submódulo), **NO obra-scoped**, y **faltaba la política UPDATE**. Consecuencia (bloqueante del caso de uso real): (a) un **ingeniero/capataz** vinculado a su obra (responsable/`proyecto_responsables`/`proyecto_empleados`) pero **sin módulo ni permiso `proyectos.personal`** podía crear el `personal_obra` + filas de fotos (esas tablas SÍ son obra-scoped por RLS) pero **NO podía SUBIR los archivos** → el registro moría en la 1ª foto; (b) el `upsert:true` del outbox fallaba al reintentar (espeja [[storage-upsert-needs-update-policy]]). **Fix:** las 4 políticas (SELECT/INSERT/**UPDATE nueva**/DELETE) ahora hacen OR con `puede_ver_/gestionar_personal_obra((storage.foldername(name))[1]::uuid)` (proyecto_id = 1er folder del path `{proyecto}/{id}/{tipo}.jpg`); los roles elevados van primero en el OR (no se afecta la web, que además gana la política UPDATE que su upsert necesitaba). Verificado en prod: 4 políticas presentes; write-path (upsert personal + 5 fotos + secuencia de carnet) probado en bloque con rollback (0 datos residuales). **Rollback:** re-aplicar las 3 políticas role-only de `2026-08-14-ar1-registro-personal-obra.sql` y `drop policy "personal-obra: actualizar" on storage.objects;`. **SGC (repo padre): ✅ mirroreada** (`SGC/sql/2026-08-15-ar1c-…`, commit `6108546`, push `main`).

### ✅ Device-QA HECHA (Claude, Huawei STK-LX3 / Android 10 / WebView Chromium 152)
Verificado end-to-end vía ADB con **Test User 3** (rol con módulo `proyectos`, no admin), contra prod: (a) **tile** "Personal de obra" en Home; (b) **lista** vacía→con datos, conteos, buscador, filtros, FAB; (c) **wizard completo 1→5**: Datos (cargo obligatorio bloquea "Siguiente" ✓; obra por `directorio_proyectos` ✓; nacionalidad DO/HT/otra ✓), Documento (número + **foto solo-cámara** real vía cámara del sistema + rehidratación del slot ✓), Fotos guiadas (**persona obligatoria**; pared/carnet/persona_carnet_cedula **opcionales** — "Siguiente" habilita solo con persona ✓), Carnet (render + **QR offline** + "Carnet pendiente" ✓), Resumen; (d) **Registrar online** → outbox → sync → **carnet emitido server** (CSD-000003) → lista+conteos actualizados ✓; (e) **expediente**: datos, Editar/Desactivar, carnet con número real, **galería 5 fotos** (2 con imagen firmada, 3 placeholders), **lightbox** ✓; (f) **Editar** inline (`personal_editar` por outbox, toast + optimista) ✓; (g) **Desactivar** (ConfirmDialog) ✓; (h) **offline** (modo avión): edición encolada ("📴 Sin señal · 1 se enviarán solos") → al volver la señal **drena** ("Todo enviado") y persiste en prod ✓. Datos de QA borrados de prod (0 residuales, bucket limpio).
- **Fix encontrado y shipeado en QA → 1.77.1:** offline, `enqueueRegistro`/`enqueueEditar` invalidaban `personal_lista` y dejaban la lista **en blanco** (sin red para recargar). Se quitó ese invalidate del cliente (el handler ya invalida tras sincronizar); verificado en device: tras editar offline la lista **sigue mostrando** al personal. Commit `0c5729c` + APK 1.77.1 firmado/registrado/subido + `publicada`=1.77.1 (1.77.0→false).
- **Follow-up (no bloquea):** el **expediente offline** tarda (skeletons) porque `getById` es network-first y espera el timeout antes de caer al cache; considerar leer cache primero / carrera con timeout corto (patrón general de la app; no exclusivo de personal).

### ⚠️ Pendientes de Xaviel (PROMPT-16 AR)
- **Shipping:** ✅ HECHO — 1.77.0 (`a65a6df`) + **1.77.1** (`0c5729c`, fix offline de QA) en `main` (PWA), APK firmado+registrado+subido, `version_publicada()`=**1.77.1**; AR1c mirroreada a SGC (`6108546`).
- **Interpretación de AR1 — CONFIRMADO por Xaviel (15/08):** (1) **fotos del carnet opcionales** ✅ (obligatorias: obra, nombre, nacionalidad, cargo, foto de la persona, y foto del documento salvo tipo "sin documento"; recomendadas: pared, carnet, persona+carnet+cédula) — así quedó en 1.77.0, sin cambios; (2) **cargo obligatorio** ✅ — así quedó en 1.77.0. **Sigue abierto:** **carnet en la app = digital en pantalla** (no imprime; la web sí imprime) — ¿hace falta compartir/descargar el carnet como imagen/PDF desde la app? (diseño del carnet = pendiente declarado).
- **⏸ Paso de firma de documentos:** `FIRMA_HABILITADA=false`. Cuando Xaviel defina qué documento(s) se firman (plantillas de Legal), poner en true (el wizard inserta el paso y el outbox ya sube la firma). Falta decidir documento(s) y si es obligatorio.
- **Edición de FOTOS en el expediente:** v1 edita solo DATOS + estado (offline). Re-tomar/reemplazar fotos desde el expediente = follow-up (el registro sí las sube; reemplazarlas luego no tiene UI aún).
- **Device-QA:** ✅ hecha por Claude (ver sección arriba) — flujo completo online + offline, con Test User 3 (módulo proyectos). **Pendiente solo lo físicamente distinto:** (a) un **capataz/ingeniero REAL obra-linked SIN módulo proyectos** registrando con fotos, para confirmar el path obra-scoped de AR1c en un teléfono suyo (verificado por RLS/lógica, no con esa cuenta en device); (b) verificar el expediente/carnet en la **web SGC** (mismo dato) escaneando el QR.

## 🟢 SESIÓN 14/08 — PROMPT-14 ronda AQ (app, 6 fases) — **RELEASE 1.76.0 PUBLICADO (rolling)**
Consume **PROMPT-13 (SGC AQ, commit `030473e`, v1.75.0)** — verificado por introspección en prod: `anular_conduce(p_salida_id,p_motivo)`, `mis_conduces_pendientes_entrega` (sin filtro `es_prueba`), `confirmaciones_historial`/`mis_entregas_por_confirmar`/`mis_confirmaciones` (destino coalesce obra↔almacén central), notificaciones con `referencia_id`/`referencia_tipo` + push de versión (trigger `app_version_push`), `estadisticas_uso`, `puede_ver_otras_bitacoras`. `npm run build` **exit 0**; `gradlew :app:compileDebugJavaWithJavac` **exit 0**. **SHIPPED:** commit **`cf52b93`** → push `main` → **PWA por Vercel**. **APK 1.76.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado) + subido al bucket (`csd-app-1.76.0.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` 1.76.0** (1.75.0→false) → `version_publicada()`=**1.76.0**; `version_minima` sigue **1.70.0**. **2 migraciones aplicadas a prod:** `sql/2026-08-14-aq9-sgc-mensajes-update-policy.sql` (aditiva — política UPDATE del bucket `sgc-mensajes` para que los re-envíos de adjuntos por outbox `upsert:true` no fallen RLS; espeja [[storage-upsert-needs-update-policy]]) y `sql/2026-08-14-publicar-1.76.0.sql` (flip). **Rollback:** `update sgc.app_versiones set publicada=(version='1.75.0') where plataforma='movil';` + `git revert cf52b93 && git push`; `drop policy "sgc-mensajes: scoped update" on storage.objects;`.

- **FASE 4 — Header roto de "Entregas por confirmar" (AQ11):** `por-confirmar` pasa a header de **dos filas** (título+atrás+🔄 arriba, "🕘 Mis confirmaciones" abajo) → el título ya no envuelve en 3 líneas ni choca. Auditados y con **truncado (ellipsis + min-width:0)** los demás headers título-largo + acción: `mensajes`, `confirmaciones`, `mi-actividad`, `conduces-historial`.
- **FASE 3 — Eliminar conduce (AQ10) + Bodega Central en confirmaciones (AQ12) + AQ5:** `ConducesService.eliminarConduce()` encola op outbox `conduce_eliminar` → handler llama **`anular_conduce`** (soft-delete server: repone stock + cancela ruta) e invalida `PENDIENTES_ENTREGA`/`CONDUCES`. En `conduce-detalle`: botón **🗑️ Eliminar** con `ConfirmDialog` de advertencia ("Se eliminará el conduce #X y se repondrá su stock. No se puede deshacer."), **gateado en cliente** (`puedeEliminar` = estado `despachado` sin receptor Y emisor|admin; el server revalida). **AQ12:** el selector de obra de `confirmaciones` ahora incluye **🏢 Bodega Central** (vía `almacenes_destino().es_central`); en modo "Mías" un central filtra por `proyecto_id null` (mis_confirmaciones no expone `destino_almacen_id`); en "Todas" el server ya matchea `proyecto_id` OR `destino_almacen_id`. **AQ5** es fix 100% backend (quitaron el filtro `es_prueba` de `mis_conduces_pendientes_entrega`) — la app ya lista ese RPC con refresco realtime (AM4); **verify-only** (device-QA).
- **FASE 2 — Chat con imágenes y archivos (AQ9):** `enviar_mensaje` + `listar_mensajes` YA cargaban `archivo_*` y el bucket `sgc-mensajes` (privado, 25 MB, RLS por participante) ya existía → se cableó la UI. `CameraService.pickAttachment()` (imágenes comprimidas ~1280/q0.7; pdf/doc/xls/… tal cual con su mime). `MensajesService.enviarAdjunto()` sube por **outbox** (bucket `sgc-mensajes`, carpeta = conversación) y el handler pasa el path subido a `enviar_mensaje` (adjunto detectado por `archivo_path`, NO por `tipo` — el CHECK solo permite texto|sistema). En el hilo: menú **＋ (📷 Cámara / 🖼️ Galería / 📎 Archivo)**, bubble optimista con preview local, **imágenes → thumbnail + lightbox**, **archivos → chip → abrir con visor del sistema** (`window.open`). Límite 25 MB validado en cliente. **Gestión de grupo (agregar/quitar/renombrar/descr/foto/promover/salir) YA estaba completa (AN6)** — no se tocó. Push "📷 Foto/📄 Archivo" = **follow-up backend** (el texto del push lo arma `enviar_mensaje`).
- **FASE 1 — Push con sonido/heads-up + deep-links (AQ1/AQ6):** **Canal Android de ALTA importancia** `avisos_csd` (sonido de notificación + vibración + heads-up) creado en `MainActivity.ensurePushChannel()` + meta-data `default_notification_channel_id` en el manifest → FCM lo usa por defecto (sonido/banner con la app cerrada). **Sonido in-app** sutil tipo WhatsApp (`NotifSoundService`, Web Audio) al llegar un aviso por realtime con la app abierta. **Deep-links** (`notifAppRoute` extendido + `referencia_id`/`referencia_tipo` en el payload/modelo): **consumo anormal → detalle de LA echada** (`/transporte/echada/:id`, del `referencia_id` o del `?echada=` de la ruta), **versión publicada → `/actualizar`**. `push.service` tap pasa las referencias. ⚠️ El canal/sonido con app cerrada **solo se valida en un APK real** (compilación Java verde, pero no ejecutada).
- **FASE 5 — Rendimiento gama baja (AQ14) — parcial:** **Detalle de echada** nuevo (`/transporte/echada/:id`, `EchadaDetallePage`) que lee `registros_combustible` directo (RLS: elevado todas, chofer las suyas), enriquece placa (embed) + nombres (`usuarios_por_ids`) + firma fotos (bucket `vehiculos`) con lightbox + flag/motivo de consumo anormal. **Registro de echadas** (`combustible-log`): filas **clickables** → detalle, y **chips de periodo rápido** 1D/1S/1M/3M/6M/1A (AQ13; presumible cierre de **AK21** — ⚠️ confirmar). **Memoria:** revocación de `object-URL` de vistas previas al reemplazar/quitar fotos en `preuso` y `checklist` (recibir/devolver) + fotos de daños → evita acumular blobs en gama baja. **PENDIENTE (perf, requiere device real):** persistencia incremental de fotos en `checklist` (preuso ya la tiene; checklist pierde fotos si lo matan — patrón AE9); el pico transitorio de base64 al armar el PDF del reporte (`preuso.buildReportData`) sigue — proponer downscale/stream; validación de ciclo completo SIN crash ×3 con métricas antes/después = **física, de Xaviel** (falta el modelo de gama baja del informe de crashes de PROMPT-13 FASE 6).
- **FASE 6 — Tutorial (AQ4):** `onboarding` rehecho: **scroll automático al elemento** (`scrollIntoView center`) antes de resaltar, recuadro **clampado al área segura** (notch/nav), tooltip que mide su **altura real** (2ª pasada por rAF) para decidir arriba/abajo sin cortarse, y card con **max-height + scroll interno con botones Siguiente/Saltar siempre visibles** (font-scale grande). Contenido de pasos sigue apuntando a los `data-tour` de Home (tiles/sync/perfil/ubicación) — vigente.

### ⚠️ Pendientes de Xaviel (PROMPT-14 AQ)
- **Shipping:** ✅ HECHO — commit `cf52b93` + push `main` (PWA), APK 1.76.0 firmado+registrado+subido, `publicada`=1.76.0. El **canal de push + sonido con app cerrada** y los **adjuntos de chat en el WebView** solo se validan en un **APK real** (device-QA abajo).
- **Device-QA (2 teléfonos + 1 gama baja):** (a) **push**: con la app CERRADA/matada, que un mensaje/entrega/ruta/**consumo anormal**/**nueva versión** SUENE + heads-up, y el tap aterrice en su entidad (echada incluida) — coordinar exclusión de batería AL5/AJ14; (b) **chat**: enviar foto (cámara/galería) y archivo (pdf), ver preview+lightbox y abrir el archivo; probar offline→outbox y un re-envío; (c) **eliminar conduce**: crear uno pendiente → 🗑️ con advertencia → desaparece de listados y el stock se repone (verificar en SGC web); (d) **AQ5**: crear conduce (online y offline) → aparece al instante en Pendiente entrega; (e) **gama baja**: ciclo completo de Uso de vehículo/inspección con fotos SIN crash ×3 + métricas antes/después.
- **Supuestos aplicados (confirmar, no bloquean):** límite de adjuntos **25 MB** (= file_size_limit del bucket); **AK21 cerrada** por AQ13 (echadas clickables + filtros); reglas de eliminación = emisor mientras pendiente + admin (las fija el server `anular_conduce`).
- **Follow-ups (no bloquean):** (1) **backend** — texto de preview del push de adjunto ("📷 Foto"/"📄 Archivo") en `enviar_mensaje`; RPC dedicada opcional de detalle de echada (hoy lectura directa RLS); (2) **perf** — persistencia incremental de fotos en `checklist` + stream/downscale del base64 del PDF de reporte; (3) **chat** — "eliminar grupo" (creador) y sustituir `confirm()` nativo por `ConfirmDialog` en gestión de grupo.

## 🟢 SESIÓN 13/08 (4) — PROMPT-12 ronda AP (app, 5 fases) — **RELEASE 1.75.0 PUBLICADO (rolling)**
Depende de **PROMPT-11 (SGC AP)**, que estaba **escrito pero NO desplegado** (SQL untracked + páginas web SGC sin commitear; último commit SGC = PROMPT-9 AO). Para desbloquear la app, se aplicaron a prod las migraciones AP **aditivas/idempotentes** vía `apply-migration.mjs` (memoria [[prompt11-ap-contracts-deployed]]): **AP1, AP2, AP3, AP4, AP4b (nueva), AP6 (nueva), AP7**. **NO se corrió el seed AP5 (sigue en PAUSA).** `npm run build` **exit 0**. **SHIPPED:** commit **`cc7319f`** → push `main` → **PWA por Vercel**. **APK 1.75.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado) + subido al bucket (`csd-app-1.75.0.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` 1.75.0** (1.74.2→false) → `version_publicada()`=**1.75.0**; `version_minima` sigue **1.70.0**. **1 migración de publicación:** `sql/2026-08-13-publicar-1.75.0.sql` (flip; sin esquema) + 7 migraciones AP aplicadas en la BD compartida SGC. **Rollback:** `update sgc.app_versiones set publicada=(version='1.74.2') where plataforma='movil';` + `git revert cc7319f && git push`. Verificado end-to-end contra prod impersonando al chofer real (POLIN RAMIREZ) y admin.

- **FASE 1 — 🔴 Obra destino vacía para el chofer (AP1):** causa raíz = el wizard leía `obras_con_bodega()` (→ **0** para el chofer) en vez del directorio. Fix app: `InventarioService.getObrasDestino()` → RPC **`directorio_proyectos()`** (ahora con lat/lng; **11 obras** para RAMIREZ verificado). El wizard usa ese contrato para la obra destino. Banda de "no vacío mudo" cuando no hay obras. `getObrasConBodega` se conserva para la devolución-de-obra (necesita `tieneBodega`). Matriz del wizard × rol chofer (verificada en prod): almacenes 20 ✓, obras 11 ✓, ferreterías 1 ✓, despachantes 14 ✓, materiales+stock 36 ✓, receptor = texto libre.
- **FASE 2 — Inventario por almacén + kardex (AP2/AP3):** nuevos métodos de servicio `inventarioAlmacen()` + `kardexArticulo()` + modelos. **Nueva página `almacen-inventario`** (`/inventario/almacen[/:bodegaId]`): artículos con existencia+apertura, buscador, toggle ocultar-cero, botón **📊 Histórico** por artículo; distingue "no tienes acceso" (42501) de "no hay datos". **Nueva página `kardex`** (`/inventario/kardex/:bodegaId/:articuloId`) = el sketch: tabla de movimientos (Mov|Origen|Destino|Fecha|Entrega|Recibe|Transporte|Conduce) en tarjetas, **timeline SVG del stock** (apertura = base, NO movimiento → sin "bajón"), filtros (tipo/transportista/quien-entrega/fechas, client-side), cada movimiento con conduce → `conduce-detalle` (fotos/firmas). Entradas: tile hub "Inventario por almacén", botón "📦 Inventario" en cada card de Almacenes, botón de histórico en `articulo-detalle`. **Stock 0 OCULTO en el `articulo-picker`** (antes solo deshabilitado) cuando hay mapa de stock; en la vista de inventario sí se ven con 0. Verificado: `inventario_almacen(Bodega Central)`=20 art (13 con stock); `kardex(PINO 2x4)` apertura 104, saldo 92, 3 movs.
- **FASE 3 — Filtros del histórico de conduces (AP4b):** `mis_conduces_historial` extendido (aditivo; se dropeó el overload de 4-args para evitar ambigüedad) con **obra origen / obra destino** (distinguibles) + **mi rol** (emisor/chofer/receptor, vía responsable_match) + devuelve origen_proyecto. UI del histórico: buscador (client), panel "Más filtros" con obra-origen/destino (CollapsibleSelect), chips de "Mi rol", rango de fechas, limpiar. Fase chips existentes se conservan.
- **FASE 4 — Rutas activas (AP6) + plataforma (AP7):** **Nueva página `rutas-activas`** (`/transporte/rutas-activas`, `moduleGuard('flota')` + `esFlotaElevado()` en cliente, tile elevado en el hub). Tab **Activas**: rutas en curso (destino, paradas, **duración corriendo**, última posición "hace X min") reutilizando `rutas_activas_y_hoy` + `choferes_estado` + realtime de Seguimiento (cero pipeline paralelo) + lista de choferes con estado. Tab **Histórico**: RPC nueva **`rutas_historial`** (elevados ven todas; filtros chofer/obra/estado/fechas) con H.I/H.F, km, duración, paradas. Detalle con trayecto → reusa el mapa de Seguimiento. **AP7:** nuevo `PlatformReportService` (booteado en `app.ts`) reporta `set_mi_plataforma(android|ios|ios-pwa|web + modelo)` al iniciar/volver al frente (best-effort, silencioso, throttle por arranque). iOS web se clasifica `ios-pwa`. Verificado (rollback): escribe plataforma+modelo+at.
- **FASE 5 — PWA de iPhone sin prompt de actualizar (AP8):** el gate/banner de actualización YA era **native-only** (`version.service` gatea por `esNativo`; APP-022) → en PWA no aparece ningún botón "Actualizar ahora". Lo que faltaba: iOS/Safari PWA no chequea el SW por su cuenta. Fix en `update.service`: **chequeo ACTIVO** `checkForUpdate()` al arrancar y en cada `visibilitychange` (foreground) → un cambio publicado se toma solo; toast sutil "Nueva versión cargada" + auto activate+reload. Cero botones de actualizar en PWA.

### ⚠️ Pendientes de Xaviel (PROMPT-12 AP)
- **Shipping:** ✅ HECHO — 1.75.0 commiteado (`cc7319f`) + push `main` (PWA), APK firmado+registrado+subido, `publicada`=1.75.0.
- **SGC (repo padre):** las migraciones AP están en **prod** pero los **archivos SQL + las páginas web SGC (`rutas-activas`, `almacen-inventario`, `kardex-modal`) siguen SIN commitear** en `dev/SGC`. En una sesión SGC: commitear todo (incluye `ap4b`/`ap6` nuevas) y desplegar la web (Hard Rule #5 — kardex/inventario/rutas-activas también en la web).
- **AP5 seed 1,000 (PAUSA):** el modelo está listo (edición solo-admin sin ensuciar el kardex); el **seed masivo NO se corrió**. Falta tu OK del alcance: ¿todas las obras? ¿Bodega Central? ¿artículos nuevos abren en 0? (asumido: obras existentes sí, nuevos en 0). La edición solo-admin de la apertura **desde la app** aún no tiene UI (se puede vía `set_apertura` RPC; la UI de la vista de proyecto es SGC/web).
- **Device-QA:** (a) chofer real Android → crear conduce a una obra (obra destino ya puebla); (b) kardex de un artículo con movimientos reales → abrir su conduce; (c) **iPhone real (PWA):** publicar un cambio menor y confirmar que la PWA lo toma sola sin pedir actualizar; (d) Conductores (web) mostrando la plataforma reportada.
- **AK21 (registro de echadas):** sigue BLOQUEADO (nota cortada); no se tocó.

## 🟢 SESIÓN 13/08 (3) — AO1 Google Maps en todo mapa — **RELEASE 1.74.2 PUBLICADO (rolling)**
Regla de Xaviel: **todo mapa de la app usa Google Maps**. Se detectó que `location-picker` (crear ruta origen/destino/paradas, y demás usos) seguía en Leaflet — nunca intentaba Google. `npm run build` **exit 0**. **SHIPPED:** commit **`02efdb7`** (feat) → push `main` → PWA por Vercel. **APK 1.74.2** firmado (cert prod `3c5316d8…5065`) + registrado (Y1) + subido al bucket + `apk_url` actualizado. **`publicada=true` 1.74.2** (1.74.1→false; verificado por flags) → `version_minima` sigue **1.42.0**. **1 migración:** `sql/2026-08-13-publicar-1.74.2.sql` (flip; sin esquema). **Rollback:** `update sgc.app_versiones set publicada=(version='1.74.1') where plataforma='movil';` + `git revert 02efdb7 && git push`.

- **location-picker → Google Maps** (espeja el patrón dual de `seguimiento`): carga vía `GoogleMapsLoaderService` (key por RPC `maps_api_key`, **confirmado no-null, 39 chars**) como primario; pin por toque **y arrastre** (dragend geocodifica); Leaflet queda SOLO como fallback offline/sin-key. Con esto los 2 únicos renders de mapa de la app (seguimiento + location-picker) usan Google. La búsqueda de texto sigue en Nominatim (migrar a Places = AO2, aún bloqueado en edge functions).
- **⚠️ Dependencia de runtime (Xaviel):** para que el mapa renderice Google DENTRO del APK (WebView, origen `https://localhost`), la key de Google debe permitir ese **HTTP referrer** (`https://localhost/*`) — ver `docs/google-cloud-checklist-ao1.md` Key B. Si el referrer no lo incluye, Google falla con RefererNotAllowedMapError y cae a Leaflet (sin romperse). **Verificar en el teléfono:** crear ruta → ¿el mapa es Google? Si sale Leaflet, falta el referrer `localhost` en la key.


## 🟢 SESIÓN 13/08 (2) — AO UX del wizard de conduce — **RELEASE 1.74.1 PUBLICADO (rolling)**
Pedido de Xaviel sobre el wizard de crear conduce. `npm run build` **exit 0**. **SHIPPED:** commit **`034ca16`** (feat) → push `main` → PWA por Vercel. **APK 1.74.1** firmado (cert prod `3c5316d8…5065`) + registrado (Y1) + subido al bucket (`csd-app-1.74.1.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` 1.74.1** (1.74.0→false) → `version_publicada()`=**1.74.1**; `version_minima` sigue **1.42.0**. **1 migración:** `sql/2026-08-13-publicar-1.74.1.sql` (flip; sin esquema). **Rollback:** `update sgc.app_versiones set publicada=(version='1.74.0') where plataforma='movil';` + `git revert 034ca16 && git push`.

- **Desplegables en hoja modal (global):** `collapsible-select` ya NO expande el listado inline empujando la página; ahora abre el listado en una **hoja inferior (bottom-sheet) deslizable + scrolleable** por encima de la pantalla, y **se cierra al elegir** (o tocar fondo/✕). El trigger/chip queda compacto en su sitio. Reusa el primitivo `bottom-sheet`; aplica a **todos** los selectores (almacenes, obras, conductor, vehículo, paradas de ruta…).
- **"Almacén central" = Bodega Central directo:** en el paso destino, elegir "Almacén central" **autoselecciona la Bodega Central** (central/principal) y muestra confirmación fija, en lugar de un picker — porque "Obra" ya cubre el almacén de cada obra (obra X ≡ almacén de obra X). "Cambiar" solo aparece si hay >1 almacén central; mensaje claro si no hay ninguno configurado (offline: usar Obra).


## 🟢 SESIÓN 13/08 — PROMPT-10 ronda AO (app, 3 de 5 fases) — **RELEASE 1.74.0 PUBLICADO (rolling)**
Depende parcialmente de **PROMPT-9 (SGC)**, aún NO aplicado → FASE 3 y FASE 4 quedan pendientes (ver abajo). `npm run build` **exit 0**. **SHIPPED:** commit **`5c4a182`** (feat) → push `main` → **PWA la despliega Vercel**. **APK 1.74.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado) + subido al bucket (`csd-app-1.74.0.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` para 1.74.0** (1.73.1→false) → **`version_publicada()` = 1.74.0**; `version_minima` sigue **1.42.0** (nadie bloqueado). **1 migración aplicada a prod:** `sql/2026-08-13-publicar-1.74.0.sql` (flip publicada; sin cambios de esquema). **Rollback:** `update sgc.app_versiones set publicada=(version='1.73.1') where plataforma='movil';` + `git revert 5c4a182 && git push` (PWA).

- **FASE 1 — AO3 stock visible + bloqueo ANTES de crear el conduce + saneo de atascados:** el bloqueo de exceso YA existía para origen almacén (paso), pero fallaba offline (existencias vacías → no bloqueaba) y no había cerrojo al emitir ni el picker sabía de stock. Cambios: `articulo-picker` con input `stock` (deshabilita/marca "Sin stock" los 0, bloquea su selección, muestra disponible); `generar-conduce` clampa la cantidad al disponible (agregar/+−/escribir), **cerrojo DURO en submit** (no emite con exceso → ya no nacen conduces que mueren en el outbox), aviso "stock del último sync" offline. **Corregir atascados:** botón **"✏️ Corregir"** en *Pendientes de envío* para `conduce_simple` con error → `SyncService.getOp/getOpFotos`, reconstruye el borrador desde el payload, **copia fotos/firmas (no se pierden)**, descarta la op y reabre el wizard en materiales. (Ruta desde *Documentación en proceso* → "Ver envíos" → Corregir.)
- **FASE 5 — AO7 responsive + aviso de WebView:** diagnóstico del caso RAMIREZ = **escala de fuente del sistema muy grande × tiles en px fijos**, NO WebView viejo (su Home renderiza → Chromium ≥ 111; el guard nativo bloquea < 111 con pantalla completa). Cambios: `home.scss` grid **fluido** (`auto-fill`/`minmax(min(150px,100%),1fr)`); `big-button` label a **2 líneas + elipsis** y encogible; **`overflow-x:hidden`** en `.screen`/body; `MainActivity` **tope de textZoom a 140%** (accesibilidad sin reventar layout); `app.ts` **aviso in-app** "actualiza Android System WebView" si el motor < 111 (red de seguridad cuando el guard nativo devuelve -1).
- **FASE 2 — AO4 compartir conduce en PDF:** el share/descargar YA estaba completo en `conduce-detalle` (jsPDF + `@capacitor/share`). Faltaba el atajo al emitir: `crearConduceSimple`/`crearConduceDevolucionSuplidor` ahora **devuelven el id**; `big-confirm` admite acción secundaria; la pantalla de éxito ofrece **"📤 Ver / compartir conduce"** (deep-link al detalle). Online-gated (el PDF necesita el número de conduce, que es server-side tras sync).

### ⚠️ Pendientes de Xaviel (PROMPT-10 AO)
- **FASE 3 (AO1/AO2 — Google Maps + Places):** BLOQUEADA. La key del mapa la sirve el RPC **`maps_api_key`** (SGC) y Places/geocoding deben ser **edge functions** (key server-side) — nada de eso está en este repo ([[maps-key-via-sgc-rpc]]). **Checklist de Google Cloud ENTREGADO Y COMMITEADO:** `docs/google-cloud-checklist-ao1.md` (commit **`fbd32f6`**) — pasos manuales de Xaviel: inventario/limpieza de keys (rotar si alguna se commiteó, AG1), **2 keys** (server-side Places/Geocoding vs Maps JS **por HTTP referrer** — ojo: la app usa el JS API en el WebView, NO el SDK nativo, así que la restricción es referrer con `https://localhost/*` incluido, no package+SHA-1), cuotas/alertas de billing, cableado a `sgc.parametros`(Key B vía `maps_api_key`) + secret Supabase (Key A). App-side (cuando exista la key): migrar `location-picker` a Google, sustituir `GeocodingService.buscar` (Nominatim) por Places autocomplete (session token + bias RD) en todos los selectores, y retirar Leaflet.
- **FASE 4 (AO6 — permisos de ubicación por rol, pedido de Eduardo NG):** BLOQUEADA por diseño. La flag **"comparte ubicación"** NO existe aún (PROMPT-9/SGC). Gatear solo por `esChofer` **rompería a Misael** (no es chofer y Eduardo lo quiere rastreado). Toda la maquinaria (helpers de rol, tracking, gate) está lista para colgarse de la flag cuando aterrice. **Falta identificar el usuario real de Misael.**
- **Device-QA (FASE 1/5):** (a) obra cuyo almacén está en 0 → NO se puede armar el conduce; (b) los 2 conduces trabados del 12/08 → "Corregir", ajustar y reenviar sin perder fotos; (c) teléfono de RAMIREZ (o font-scale grande en emulador) → Home ya no se desborda ni corta "Notas"/"Mensajes".
- **AK21 (registro de echadas):** sigue BLOQUEADO (nota cortada); no se tocó.


## 🟢 SESIÓN 12/08 — Arreglos AN (Bodega Central en rutas + UI) — **RELEASE 1.73.1 PUBLICADO (rolling)**
Reporte de campo (usuario RAMIREZ, chofer): (1) al crear una **ruta** no aparecía **Bodega Central** en la lista de origen/destino; (2) "la UI está bugueada en otras pantallas". `npm run build` **exit 0**. **SHIPPED:** commit **`be98e96`** (fix) → push `main` → **PWA la despliega Vercel**. **APK 1.73.1** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado) + subido al bucket (`csd-app-1.73.1.apk` + `latest` + `version.json`, `apk_url` actualizado). **`publicada=true` para 1.73.1** (1.73.0→false) → **`version_publicada()` = 1.73.1**; `version_minima` sigue **1.42.0** (nadie bloqueado). **1 migración aplicada a prod:** `sql/2026-08-12-publicar-1.73.1.sql` (flip publicada; sin cambios de esquema). **Rollback:** `update sgc.app_versiones set publicada=(version='1.73.0') where plataforma='movil';` + `git revert be98e96 && git push` (PWA).

- **Bug 1 — Bodega Central no seleccionable al crear ruta (ARREGLO):** `crear-ruta.ts` `lugarOpts` filtraba `tipo==='obra'` (resabio de AF24.2) y ocultaba **todos** los almacenes sueltos, aunque los tres selectores (origen/destino/paradas) ya decían "Obra o almacén". `destinos_transporte()` ya devuelve Bodega Central como `tipo:'almacen'` y NUNCA expone el almacén implícito de una obra → sin duplicado. Se reincluyen con icono 🏢 (obras 🏗️). La lógica aguas abajo (`selectedLugar`, `crearRuta` con `destinoProyectoId = tipo==='obra' ? id : null`) ya trataba bien los almacenes: una ruta a Bodega Central lleva destino nombrado + coords y proyecto null, como en la web.
- **Bug 2 — "UI bugueada" (revisión + ARREGLOS):** la "caja gris" del header en la foto era reflejo de sol en pantalla rota, no bug (Home OK). Barrido completo de SCSS/HTML de `pages/`+`shared/` (los dark-cards legacy ya estaban reparados). Hallazgos reales corregidos: (a) **rojo de peligro perdido en 8 sitios** — `--color-danger` se usaba SIN fallback (indefinido → `color` inválido → heredaba color oscuro) en botones de borrar y el `*` de campo obligatorio (Notas editor, Conduces, Crear ruta, Generar conduce, Ferretería, Devolver material). Fix: alias `--color-danger: var(--color-error)` en `:root` (mismo patrón que `--primary`/`--main`). (b) **footers/barras flex** con botones aplastados: `almacenes` (confirmado — "Cancelar" acaparaba y "Guardar" se encogía), `transporte` y `home` edit-bars (hint del centro aplastado). Fix: `width:auto` en los botones de esas filas. **No tocado (drift cosmético, sí renderiza por fallback):** `--muted`, `--border`, `--surface-2`, `--Hub-translucid`, `--color-warning-text/-border`, `--fs-caption`, `--secondary` — candidatos a un cleanup de tokens en `:root` si se quiere.

### ⚠️ Pendientes de Xaviel (1.73.1)
- **Publicar:** ✅ HECHO — `publicada`=1.73.1 (`sql/2026-08-12-publicar-1.73.1.sql`). Los usuarios ya reciben el aviso de actualización.
- **Device-QA:** crear una ruta y confirmar que **Bodega Central** aparece y es seleccionable como origen/destino/parada, y que la ruta a Bodega Central sincroniza (offline y online). Verificar de paso que los botones de borrar/`*` se ven en rojo y que "Guardar almacén" ya no sale aplastado.

## 🟢 SESIÓN 11/08 (tarde) — PROMPT-8 ronda AN (app, 3 fases) — **RELEASE 1.73.0 PUBLICADO (rolling)**
Depende de **PROMPT-7 (SGC AN)** — verificado en prod por introspección (rule [[prompt9-contract-gaps-ah]]): `directorio_empleados/usuarios_detalle/proyectos`, policies de referencia en `bodegas`/`stock_por_bodega`, `submodulo_nivel_explicito` + policies granulares en las 16 tablas inventario/compras/flota, y todo el set de `grupo_*` + columnas `conversaciones.descripcion/avatar_path`, `conversacion_participantes.rol`, `mensajes.tipo` — **TODO aplicado**. `npm run build` **exit 0**. **SHIPPED:** commit **`4127baf`** → push `main` → **PWA desplegado por Vercel**. **APK 1.73.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado) + subido al bucket (`csd-app-1.73.0.apk` + `latest` + `version.json`, `apk_url` seteado). **`publicada=true` para 1.73.0** (1.72.1→false) → **`version_publicada()` = 1.73.0**, `version_minima` sigue en **1.42.0** (nadie bloqueado). **2 migraciones aditivas aplicadas a prod:** `SGC/sql/2026-08-11-an6b-listar-mensajes-tipo.sql` (commit SGC `1481d6e`, ver abajo) y `sql/2026-08-11-publicar-1.73.0.sql` (flip publicada). **Rollback:** `update sgc.app_versiones set publicada=(version='1.72.1') where plataforma='movil';` + `git revert 4127baf && git push` (PWA).

- **FASE 1 — Datos de referencia con roles restringidos (AN3/AN1):** **el bug de prod ("almacenes de origen vacíos" al quitarle Inventario al Chofer) queda ARREGLADO server-side por PROMPT-7 — sin cambio de app.** Verificado empíricamente simulando la RLS de un Chofer real (Papo, `modulos=[flota,transporte]`, sin inventario): bodegas **20**, stock_por_bodega **36**, articulos **257**, proyectos **11**, `destinos_transporte()` **20**, `directorio_usuarios_detalle()` **20**. El wizard de conduce ya usa RPCs/tablas ahora legibles (origen=`bodegas` directo, destino=`obras_con_bodega`/`almacenes_destino` RPC, despachante RPC). `ConducesService.getProyectos` (`.from('proyectos')`) **NO se migró**: la RLS de `proyectos` permite `transporte`/`flota` (chofer lee 11) y `directorio_proyectos()` no trae `latitud/longitud` que los callers necesitan. **1 fix de app:** `TecnologiaService.getEquipos` dejaba el embed `empleado:empleados(...)` → el rol Tecnología (solo módulo `tecnologia`, sin rrhh) veía "Asignado a" en null; ahora resuelve el nombre por `directorio_empleados()` (best-effort). `directorio_empleados()` devuelve **1** fila = correcto (solo hay 1 empleado activo no-prueba; dato, no bug).
- **FASE 2 — Gating por submódulo Ver/Operar (AN2, Flota + Inventario):** la infra ya existía (`submoduleGuard(clave,nivel)` + `puedeVer/OperarSubmodulo`, espejo compat de `nivel_submodulo`). Se **cambiaron los guards de ruta** de `moduleGuard('flota'|'inventario')` → `submoduleGuard(...)` en: **Flota** vehículos lista/perfil + conductores lista/perfil (Ver); vehículos/conductores nuevo/editar (Operar). **Inventario** existencias + articulo/:id + conteos-historial (Ver); salida/entrada/recibir/conteo (Operar). **100% aditivo y retrocompat**: quien tiene el módulo padre → `nivelSubmodulo`='operar' → pasa igual que antes (chofer/jefe_flota/inventario intactos); un rol GRANULAR (ej. `abogado` con `flota.vehiculos:ver`) ahora **puede** llegar a la lista en solo-lectura. Los botones de acción (Agregar vehículo/conductor) ya estaban gateados por `esAdmin()` → un rol Ver no los ve. **Pendiente (follow-up, no bloquea):** surfacing de navegación para roles solo-Ver (tile en Home / hub) — hoy la app es de módulo completo; un rol Ver llega por deep-link o si se le agrega un tile. Documentado a propósito, no es hueco silencioso.
- **FASE 3 — Grupos de Mensajes tipo WhatsApp (AN6):** consumidos los contratos de PROMPT-7 FASE 5. **Nueva pantalla `/mensajes/:id/info`** (`GrupoInfoPage`): avatar (editable por admin: cámara/galería → `sgc-mensajes` bucket, path único por cambio → sin política UPDATE → `grupo_set_avatar`), nombre+descripción editables (admin, `grupo_editar`), lista de participantes con chips Creador/Admin, agregar (buscador `buscar_usuarios` → `grupo_agregar`), quitar (`grupo_quitar`), promover/degradar (`grupo_promover`), **Salir del grupo** (`grupo_salir`, cualquiera). **Thread**: header ahora carga la meta (nombre real + avatar) y es **tappable en grupos** → abre la info; los **eventos de sistema** (`tipo='sistema'`, "X agregó a Y") se pintan como pastilla centrada, no como mensaje. **Inbox**: `＋` ahora tiene toggle **Persona | Grupo** → crear grupo en la app (nombre + multi-selección → `crear_grupo`; antes solo se creaban en la web). Verificado end-to-end contra prod (tx con rollback): creador=admin, editar/agregar/promover OK, `grupo_info`=3 participantes, **3 eventos de sistema** legibles vía `listar_mensajes` con `tipo`.

### Migración AN6b (aplicada a prod, aditiva/retrocompat)
`SGC/sql/2026-08-11-an6b-listar-mensajes-tipo.sql` — **hueco de contrato de AN6**: la migración AN6 agregó `mensajes.tipo` e inserta eventos con `grupo_evento()`, PERO `listar_mensajes` NO devolvía la columna → los eventos se veían como mensajes normales. El fix DROP+CREATE agrega `tipo` a la salida (coalesce 'texto'), regrant a PUBLIC. Web no se afecta (lee por nombre). **Rollback:** re-crear `listar_mensajes` sin la columna `tipo` (definición previa en git/pg).

### ⚠️ Pendientes de Xaviel (PROMPT-8 AN)
- **Shipping:** ✅ HECHO — 1.73.0 commiteado (`4127baf`) + push `main` (PWA), APK firmado+registrado+subido, `publicada`=1.73.0.
- **Device-QA:** (a) **grupos**: crear grupo en la app, tocar el header → info, cambiar foto (cámara/galería), agregar/quitar/promover, ver los eventos de sistema en el hilo, salir; en 2 teléfonos que el otro miembro vea los eventos por realtime. (b) **rol restringido real**: con un Chofer sin Inventario, crear conduce completo (almacenes y materiales pueblan) — confirmar en campo lo ya verificado por RLS. (c) opcional: darle a un rol de oficina `flota.vehiculos:ver` y confirmar que llega a la lista de vehículos en solo-lectura por deep-link.
- **AN2 follow-up (no bloquea):** decidir si se surfacea navegación (Home/hub) para roles solo-Ver, o si se queda como acceso por deep-link/futuro.
- **AK21 (registro de echadas):** sigue BLOQUEADO (nota cortada); no se tocó.

## 🟢 SESIÓN 11/08 (tarde) — PROMPT-6 ronda AM (app, 4 fases) — **RELEASE 1.72.1 PUBLICADO (rolling)**
**Actualización 1.72.1:** se retiró el origen **"Otros"** del conduce (AM6 — camino muerto: crasheaba pre-AM1 por `bodega_id` NOT NULL y daba DR454 ahora; incompatible con el descuento de stock). Origen del conduce = Almacén/obra o Ferretería. Commit **`f0cbcc6`**, APK 1.72.1 firmado+registrado+subido, `publicada`=1.72.1. **AM8/AM10 backfill YA estaba aplicado en prod** (las 8 obras CSD-101..108 tienen lat/lng + `direccion_geo` [link Maps] + `ubicacion_metodo='backfill'` + ingeniero/maestro/contacto + descripción limpia; los únicos activos sin ubicación son 3 de prueba/junk sin link → nada que backfillear). Rollback 1.72.1→1.72.0: `update sgc.app_versiones set publicada=(version='1.72.0') where plataforma='movil';` + `git revert f0cbcc6 && git push`.

Consume **PROMPT-5 (SGC AM, `e475c3d`, v1.72.0)** — verificado en prod: `resolver_bodega_origen`, `crear_conduce_devolucion_suplidor`, `conduce_iniciar_ruta`, `mis_conduces_pendientes_entrega` ampliada, trigger `trg_salidas_protege_portador`, `set_proyecto_ubicacion`, `proyectos_sin_ubicacion`, campos estructurados de `proyectos`, edge `resolve-maps-link` (ACTIVE). `npm run build` **exit 0**. **SHIPPED:** commit **`af0da65`** → push `main` → **PWA desplegado por Vercel**. **APK 1.72.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado) + subido al bucket (`csd-app-1.72.0.apk` + `latest` + `version.json`, `apk_url` seteado). **`publicada=true` para 1.72.0** (1.71.0→false) → **`version_publicada()` = 1.72.0**. `version_minima` sigue en **1.70.0** (sin cambio; nadie nuevo bloqueado). **1 migración aplicada a prod:** `sql/2026-08-11-publicar-1.72.0.sql` (flip publicada). El resto del backend AM ya estaba en prod por PROMPT-5. **Rollback:** `update sgc.app_versiones set publicada=(version='1.71.0') where plataforma='movil';` + `git revert af0da65 && git push`.

- **FASE 1 — Conduces confiables (AM1/AM3/AM4/AM5):**
  - **AM1:** la devolución a suplidor ya NO va por `conduce_simple` con el suplidor en observaciones; usa el contrato dedicado **`crear_conduce_devolucion_suplidor`** (origen bodega OBLIGATORIO → cierra el bug `bodega_id null`). Nuevo `tipo_op` de outbox `conduce_devolucion_suplidor`. Los errores estructurados **DR451-454** ahora se clasifican como `validacion` permanente en `throwSyncError` (regex `^DR\d`) → se muestran **verbatim y accionables** en Pendientes de envío, sin reintentar en bucle, sin perder fotos. El wizard ya gateaba origen/stock/datos por hoja (verificado).
  - **AM3 (re-reporte AL9):** row→detalle YA estaba correcto en código (el cuerpo del card es un botón `verDetalle` → `conduce-detalle`, con empty-state + retry si falla/offline). Shipeó en 1.71.0; el re-reporte fue APK viejo. Sin cambio de código; verificado.
  - **AM4 (re-reporte AL13):** el handler `conduce_transf_aceptar` ahora **invalida también `CATALOG_PENDIENTES_ENTREGA`** (antes se omitía → bandeja inconsistente). Nuevo **realtime de avisos**: `NotificacionesService.iniciarRealtime()` (canal `sgc.notificaciones` filtrado por usuario, en publicación supabase_realtime) bombea `tick`/`lastTipo`; se arranca en `app.ts` (constructor + resume + 1ª nav fuera de /auth), se cierra en `session.logout()`. `conduces-pendientes` refetchea al `tick` con `lastTipo==='transporte'` → emisor ve desaparecer y receptor aparecer **al instante** en ambos teléfonos. La app **nunca** hace PATCH directo de `conductor_id` (verificado; el trigger lo blinda de todos modos).
  - **AM5:** cableado el ciclo conduce→ruta. Modelo `ConducePendienteEntrega` ampliado (`ruta_id/ruta_estado/vehiculo_id/motivo/tiene_ruta`). Botón **"🚚 Iniciar ruta"** (o chip "🟢 En ruta" si `ruta_estado==='en_curso'`) en cada conduce de Pendiente entrega → `conduce_iniciar_ruta` (online). Si el conduce no trae vehículo, abre un selector de vehículo inline (maneja DR461). Tras iniciar → aparece en Mis rutas + Seguimiento.
- **FASE 2 — Refresh homologado (AM2):** nueva **directiva reutilizable `LiveRefreshDirective`** (`shared/ui/live-refresh`, `[appLiveRefresh]`): pull-to-refresh + refetch al volver a foreground (App.resume + visibilitychange) con indicador de pull gestionado por la directiva; el botón 🔄 visible queda en cada header. Aplicada a **10 listados vivos**: conduces-pendientes (ya lo tenía inline), Mis rutas (conduces), por-confirmar, confirmaciones, avisos (campana), mi-actividad, avisos de flota, mensajes (lista), conduces-historial, y Proyectos (lista). Seguimiento se dejó a su realtime (no botón; mapa vivo). Los loaders aceptan `silent` para no parpadear el skeleton en refrescos automáticos.
- **FASE 3 — Proyectos v1 (AM9/AM7/AM10):**
  - **UI homologada a tema CLARO** (cierra AK5): lista y detalle ya no usan cards `#161616`/`#212121`/`--main-ultra-light`; todo con tokens `--color-surface/--color-border/--color-text-*`.
  - **Crear/editar proyecto por HOJAS** (`proyectos/nuevo`, `proyectos/:id/editar` → nuevo `ProyectoFormPage`): Datos → Ubicación → Equipo/contacto → Descripción+revisar (step-bar + wizard-footer). Gating por rol (`esAdmin || hasModulo('proyectos'|'direccion')`), igual que la web.
  - **Ubicación fácil (AM7):** pegar **link de Google Maps** (incl. cortos `maps.app.goo.gl`) o **coordenadas** → edge `resolve-maps-link` → pin; o mover el pin en el **LocationPicker** Leaflet (ya existía, reusado). Se fija con `set_proyecto_ubicacion` (validado). **Requerida al crear** (validación de form, AM8).
  - **Data estructurada (AM10):** `ingeniero_obra`, `maestro_encargado`, `contacto_nombre`, `contacto_telefono` (clicable `tel:`) en sus campos/cards del detalle; ubicación con **"Abrir en Google Maps"**; descripción = solo descripción. Sección **"Equipo y responsables"** (`responsables_de_proyecto`).
- **FASE 4 — Regresión conduces (AM6):** matriz origen×destino a nivel código (build + introspección de prod): almacén→obra ✓, almacén→almacén central ✓, almacén→suplidor ✓ (RPC dedicada), ferretería (compra) ✓. **Hallazgo:** el origen **"Otros"** (items=[]) devuelve DR454 "no materials" — todo conduce funnelea a `crear_conduce_transportista` que exige ≥1 item; "Otros" también crasheaba pre-AM1 (NOT NULL), o sea es un camino muerto de larga data → **decisión de Xaviel** (¿"Otros" lleva items o se elimina?). Ver memoria [[conduce-creation-funnel]].

### ⚠️ Pendientes de Xaviel (PROMPT-6 AM)
- **Shipping:** decidir commit/push (PWA) + `npm run apk`/`apk:publish` + flip `publicada`. NO se hizo (regla madre).
- **Device-QA OBLIGATORIO (2 teléfonos reales):** (a) **AM4 transferencia**: emisor y receptor en teléfonos distintos → al aceptar, desaparece del emisor y aparece completo en el receptor **al instante** (realtime); (b) **AM1 caso 911→ferretería**: devolución a suplidor end-to-end + ver que un error de outbox se muestre claro (DR451-454) con reintento sin perder fotos; (c) **AM5**: crear conduce → Iniciar ruta → verlo vivo en Mis rutas + Seguimiento → Estoy entregando; (d) **AM7**: crear proyecto pegando un link `maps.app.goo.gl` real → pin correcto.
- **Decisión AM6 "Otros":** RESUELTO — se eliminó en 1.72.1 (era camino muerto).
- **AK21 (registro de echadas):** sigue BLOQUEADO (nota cortada); no se tocó.
- **Follow-ups Proyectos:** backfill de ubicaciones desde descripciones (AM8, hay `sql/2026-08-11-am8-am10-backfill-ubicaciones-APLICAR-TRAS-OK.sql` en SGC, pendiente de tu OK) + migración asistida de descripciones→campos (AM10) son tareas de SGC/admin, no app. Expediente/Ejecución/Costos/Partidas/Cuadre/CL siguen solo en web (alcance app v1 acordado).

## 🟢 SESIÓN 11/08 — PROMPT-4 ronda AL (app, 7 fases) — **RELEASE 1.71.0 PUBLICADO (rolling)**
Consume PROMPT-3 (SGC AL, `75b58f6`, v1.71.0 — ya en prod). `npm run build` **exit 0**; `gradlew compileDebugJavaWithJavac` **exit 0** (plugin nativo de alarma). **SHIPPED:** commit **`d9c3c1c`** → push `main` → **PWA desplegado por Vercel**. **APK 1.71.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado por módulo) + subido al bucket (`csd-app-1.71.0.apk` + `latest` + `version.json`, `apk_url` seteado). **`publicada=true` para 1.71.0** → **`version_publicada()` = 1.71.0**. ⚠️ **`version_minima` = 1.70.0** (venía así desde la release AK; NO lo cambié — quien esté por debajo de 1.70.0 ve el gate bloqueante; bajar `minima` si se quiere un rollout más suave). **2 migraciones aditivas aplicadas a prod:** `sql/2026-08-11-al2-tec-equipo-app.sql` (RPC `guardar_tec_equipo_app` — UPSERT idempotente + código TEC-#### para el inventario tecnológico; no toca la web) y `sql/2026-08-11-publicar-1.71.0.sql` (flip de publicada). **Rollback:** `update sgc.app_versiones set publicada=(version='1.70.0') where plataforma='movil';` + `git revert d9c3c1c && git push`.

- **FASE 1 (AL13/9/12/10) — Conduces día a día:** `conduces-pendientes` ahora tiene **pull-to-refresh + botón Actualizar + refresco al volver a foreground** (App.resume + visibilitychange) → arregla el bug del emisor que seguía viendo el conduce transferido (el RPC `mis_conduces_pendientes_entrega` ya está anclado al portador; basta re-leer). **Rows clickables** → nueva página **`conduce-detalle`** (`/transporte/conduce-detalle/:salidaId`, auth+pin, sin flota-guard) que consume `conduce_detalle_app` (documento con items/firmas/fotos/transferencias, portador ACTUAL). Wizard de crear conduce: **resumen con detalle de materiales** (nombre+cant+unidad, AL12) y **Bodega Central/almacenes centrales como destino** (`almacenes_destino` + `crear_conduce_simple` 15-arg con `p_destino_almacen_id`). Histórico: botón "Ver conduce" → detalle.
- **FASE 2 (AL4) — Ver conduce + PDF:** nuevo `ConducePdfService` (jsPDF, self-contained, sin CDN) que reproduce el formato del conduce de la web (SGC usa `window.print()`, no hay lib PDF que copiar). Botones **Compartir** (share sheet Capacitor → WhatsApp con el PDF) y **Descargar PDF** en `conduce-detalle`. jsPDF cae en el chunk lazy del detalle (no infla el bundle inicial). Sin red → aviso claro.
- **FASE 3 (AL8) — Confirmador:** `por-confirmar` ahora abre el **detalle completo del conduce ANTES de confirmar** (botón "Ver conduce (lo que debía llegar)") + link "Mis confirmaciones". Página `confirmaciones` con toggle **Mías / Todas** (Mías = `mis_confirmaciones`; Todas = `confirmaciones_historial`). **Entrada permanente**: los de flota la tienen en el hub; para confirmadores sin módulo flota se añadió un **tile de Home "Por confirmar"** (badge real) visible a inventario/obra o si hay pendientes.
- **FASE 4 (AL1/AL2) — Tecnología + Administración completos:** módulo **Tecnología** = inventario tecnológico real (`/tecnologia-inventario`, gate `moduleGuard('tecnologia')`): lista con buscador+filtros, alta/edición **por hojas** (datos→tipo dropdown+CRUD→ubicación→precio+moneda→**multi-fotos con portada**→resumen), detalle con **lightbox**, **offline por outbox** (`tec_equipo_guardar` → RPC nuevo). La pantalla vieja "Tecnología" (versiones/dudas/errores) se **renombró a "Sistema"** (tile `SISTEMA_TILE`, ruta `/tecnologia` intacta). **Administración completo**: `admin/usuarios` (invitar via edge fn, roles, activar/desactivar, reset contraseña), `admin/roles` (matriz AG12: módulos + submódulos ver/operar, crear/editar/eliminar), `admin/parametros` (editar `sgc.parametros`), + link a Empleados/RRHH. `TecnologiaService` booteado en app.config (S30).
- **FASE 5 (AL5/AL6) — Permisos + alarma autónoma:** onboarding de permisos **de una sola vez** (`PermisosOnboarding` en app.html, flag `csd_permisos_onboarding_v2`, nativo): cadena cámara/mic/ubicación/notificaciones + deep-links a Ajustes (ubicación "todo el tiempo", alarmas exactas, batería). **Alarma dominical AUTÓNOMA** (suena con la app CERRADA): plugin nativo **`AlarmScheduler`** + `WeeklyAlarm` (AlarmManager `setExactAndAllowWhileIdle` + notificación full-screen canal `alarma_inspeccion` con sonido de alarma/vibración) + `AlarmReceiver` (re-suena cada 30 min de 9:00 a 20:00 dom, luego próximo domingo) + `BootReceiver` (re-arma tras reboot). La app **arma** (`enable`) cuando hay inspección pendiente y **cancela** (`disable`) al completarla (app.ts `syncAlarmaNativa` en arranque+resume). Push maneja el tipo nuevo `alarm-weekly-inspection`. Manifest: `SCHEDULE_EXACT_ALARM`, `USE_EXACT_ALARM`, `USE_FULL_SCREEN_INTENT`, `RECEIVE_BOOT_COMPLETED`, `VIBRATE`, `POST_NOTIFICATIONS`.
- **FASE 6 (AL7) — Mantenimientos por chofer:** el chofer ya podía abrir mantenimiento de su vehículo en uso (moduleGuard flota); se ampliaron los **tipos a 13** (rutina/reparación/tintado/bombillo/gomas/batería/etc.), se agregaron **costo + taller + notas** al wizard (`crear_mantenimiento_app` 13-arg) y el historial muestra **`registrado_por`**.
- **FASE 7 (AL14) — Notas:** fix de **overflow** (`overflow-wrap:anywhere` + `min-width:0`) en cards de Notas, editor, mensajes y tareas; y fix del bug de la insignia **"⏳ Enviando… para siempre"** (una op en `estado:'error'` ya no se muestra como "enviando" → ahora "⚠️ No se envió"; `notasPendientes` filtra por estado).

### ⚠️ Pendientes de Xaviel (PROMPT-4 AL)
- **Shipping:** ✅ HECHO — commit `d9c3c1c` pushed a `main`, APK 1.71.0 firmado+registrado+subido, `publicada`=1.71.0. Pendiente opcional: revisar `version_minima`=1.70.0 (bajarla si algún equipo quedó bloqueado y no debería).
- **Device-QA OBLIGATORIO (2 teléfonos):** (a) transferencia Eduardo→Papo: al aceptar, desaparece del emisor al refrescar/foreground, con código+materiales completos; (b) confirmador ciego: chofer entrega → al confirmador le llega a la bandeja (tile permanente) → ve el conduce → confirma → queda en "Mis confirmaciones"; (c) **compartir conduce por WhatsApp real** (Android) y documentar el comportamiento en PWA iOS; (d) **alarma del domingo con la app MATADA** (domingo simulado): cambiar fecha del teléfono a domingo 9am con inspección pendiente → debe sonar full-screen; conceder SCHEDULE_EXACT_ALARM + batería en el onboarding. **Es físico/tuyo.**
- **Supuestos aplicados (confirmar):** tz del dispositivo = RD para la alarma; ubicación de equipos tec = dropdown de bodegas (Bodega Central = "Oficina Central"); reset de contraseña de usuario = enlace por correo (no PIN); `redirectTo` de invitación/reset = `https://app.sgcconstructorasd.com/auth/set-password`.
- **AK21 (registro de echadas):** sigue BLOQUEADO (nota cortada); no se tocó.

## 🟠 SESIÓN 10/08 (tarde) — PROMPT-2 ronda AK (7 fases) — **BUILD VERDE, SIN COMMIT/PUSH/APK aún**
Depende de PROMPT-1 (SGC v1.70.0, migraciones AK en prod). `npm run build` **exit 0**. **NO** se ha hecho commit/push/deploy ni APK (esperando OK de Xaviel). **1 migración aditiva aplicada a prod:** `SGC/sql/2026-08-10-ak22-mis-rutas-hoy-creado.sql` (añade `creado_en` a `mis_rutas_hoy`; retrocompatible).

- **FASE 1 (AK12/9/3/11/7/22):** arranque SIEMPRE en Home (se eliminó el auto-entrar mono-módulo en `home.ts` que abría Transporte al chofer en cada arranque; +limpieza `session.service`). Back del wizard de conduce → `conduces-hub` vía `navGuard.back` (antes iba a `/transporte/conduces` = Mis rutas). Empty state de Mis rutas ahora habla de RUTAS. Renombres: "Marcar entrega"→"Gestionar entrega", "Iniciar tránsito"→"Iniciar ruta" (+"En ruta" en estados/historial), "Estoy entregando" abre DIRECTO el proceso de entrega. Fecha+hora 12h de emisión (conduce) y de planificación (ruta) en las tarjetas.
- **FASE 2 (AK6):** raíz encontrada — `CollapsibleSelect` NO era cerrado por defecto (mostraba la lista inline hasta elegir). Ahora es un dropdown de verdad (trigger cerrado → abre al tap → colapsa con selección + Cambiar) → arregla TODOS los selectores del wizard de conduce de un golpe. `vehiculo-picker` tiene modo `dropdown`; los selectores de vehículo (combustible/aviso/multa/rutas) ya eran gate-screens/hojas que colapsan. Conductor de crear-ruta migrado a dropdown. Despachantes: filtro server-side (AJ6) verificado — Eduardo NG probablemente sale por rol `gerente_produccion` (obra, elegible); si es pura oficina, es dato/config (ver pendientes).
- **FASE 3 (AK8/1/4):** menú de Conduce depurado (fuera Recibir / Por firmar / Devolver a suplidor / Compra en ferretería; **rutas conservadas** para no romper deep-links). Nueva vista **Confirmaciones** (`/transporte/confirmaciones`, RPC `confirmaciones_historial`/`confirmacion_detalle`, con detalle: items pedido/recibido, quién entregó/confirmó, fotos, firmas). Badge "por confirmar" ya consume la matriz nueva server-side (sin cambio app).
- **FASE 4 (AK14/15/20/19):** modelo "en uso/libre". Nueva pantalla **Uso de vehículo v2** (`/transporte/uso-vehiculo[/:id]`): nivel de gasolina (E,1/4,1/2,3/4,F), inicia sesión / **recibe de X** (DR409) / **suelta**. Card del hub: "En uso" con Soltar; "Para usar" reemplaza "Por recibir"; fuera "Asignados a mí", "Recibir vehículo", "Checklist de pre-uso", "Asignarme". `VehiculoUsoService` nuevo. Mi actividad desbloqueada con `es_conductor_ampliado` (arregla a Papo). (`asignarme`/`preuso` quedan solo por ruta/deep-link.)
- **FASE 5 (AK16/17/18):** **Multas** endurecidas app-side: chofer→conductor preseleccionado y **bloqueado** (sin selector); elevado→`conductores_para_multa()` (incluye a Papo); gating por `puede_multar_a_otros()`. **Aviso de vehículo**: nota de **voz** (voice-notes AH13, entidad `aviso_flota`) + **video** (input cámara, bucket vehiculos, `p_videos`) + pestaña **"Mis reportes"** (`mis_novedades_reportadas`, con estado). **Historiales**: "Mis usos de vehículo" (sesiones `mis_usos_vehiculo`) en Mi actividad.
- **FASE 6 (AK10/2/5):** **Alarma dominical** in-app tipo despertador (`AlarmaService`+`AlarmaHost`, overlay full-screen + beep Web Audio + vibración): dispara con la push `alarma-reporte-semanal` en primer plano y al abrir en domingo con reporte pendiente; botones "Hacer reporte ahora"/"Posponer 1h". **Layout**: `module-order.service` ahora soporta `scope` (get/set_module_order p_scope), home/transporte intactos. **Proyectos**: arreglado el back invisible de `cronograma-avisos`.
- **FASE 7 (AK13):** app-side — permiso `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` en el manifest + aviso una-vez al iniciar ruta (instrucciones de exclusión de batería MIUI). Pipeline (foreground service + ruta_id + outbox + `registrar_posiciones`) ya estaba; server con guard ampliado + diagnóstico.

### ⚠️ Pendientes de Xaviel (PROMPT-2 AK)
- **Shipping:** decidir commit/push (PWA) + `npm run apk`/`apk:publish` + flip `publicada`. NO se hizo automáticamente.
- **AK13 validación OBLIGATORIA:** conducir un trayecto real con el user-test y confirmar en Seguimiento (vivo + breadcrumb). Es físico/dispositivo. Si MIUI mata el servicio, excluir batería con las instrucciones del aviso.
- **AK21 (registro de echadas):** BLOQUEADO — la nota llegó cortada; no se tocó.
- **Confirmar textos/valores asumidos:** AK11 "Gestionar entrega"; escala gasolina E,1/4,1/2,3/4,F; AK10 posponer 1h; matrices AK4/AK16/AK17/AK18 (defaults server aplicados); límite video 60s (hoy no forzado client-side).
- **Parciales (follow-up):** (a) AK2 — botón "Ordenar y redimensionar módulos" solo en home+transporte; el servicio ya soporta scope, falta el modo edición en el resto de módulos/submódulos; (b) AK16 — la bandeja de gestión in-app (`/transporte/avisos`) aún no muestra voz/video en el detalle (la web sí); (c) AK18 — el historial de elevados sigue con el RPC viejo (`getChecklistsHistorial`), no el nuevo `historial_checklists_vehiculo` con filtro usos; (d) AK5 — Proyectos usa tarjetas oscuras (#161616) sobre tema claro (inconsistencia de diseño): NO convertidas (cambio mayor, a decidir con Xaviel); (e) AK16 alarma full-screen con app CERRADA necesita plugin nativo (hoy es in-app/al abrir).
- **Despachante AK6:** si Eduardo NG (u otra gente de oficina) sigue saliendo mal, revisar sus roles en SGC (o `puede_despachar`); el filtro server ya está.

## 🟢 SESIÓN 10/08 — PROMPT-15 AJ15 (auditoría QA completa) — **RELEASE 1.69.0 PUBLICADO (rolling)**
Auditoría a nivel de código de toda la app (108 páginas) con 6 auditores paralelos → **`INFORME-AUDITORIA-APP.md`** (44 hallazgos QA-1..QA-44, priorizados, con evidencia `archivo:línea` y estado final en §9). **Se resolvió todo el backlog** a pedido de Xaviel: ~35 corregidos, 6 verificados como ya-correctos en backend (no eran bugs), 3 aceptados/diferidos, 1 pendiente de decisión de producto (QA-42 término "pre-uso"). `npm run build` **VERDE**. Commits: **`c9b40ea`** (TOP-3 + informe + UI), **`9e10db0`** (backlog QA-6..44), **`9395a21`** (bump 1.69.0). **Push a `main` hecho → PWA desplegado por Vercel. APK 1.69.0** firmado (cert prod `3c5316d8…5065`), registrado (Y1) y subido al bucket. **`publicada=true` para 1.69.0** (1.68.0→false) → **`version_publicada()` = 1.69.0**, `minima` = 1.42.0 (nadie bloqueado).
- **3 migraciones aditivas/retrocompatibles aplicadas a prod:** `sql/2026-08-10-qa5-traspaso-idempotente.sql` (p_id idempotente), `sql/2026-08-10-qa17-proyectos-pickables.sql` (picker de obras para compras/bitácora), `sql/2026-08-10-qa13-por-confirmar-items.sql` (items[] en la bandeja del receptor). No rompen la web (callers viejos pasan NULL / ignoran la columna nueva).
- **Fixes destacados:** colisión de handlers de outbox `tarea_*` (crítico), 5 servicios fuera del bootstrap (offline atascado, S30), traspaso no idempotente, conduce invisible offline, desvío de vehículo que borraba foto+firmas, back que moría/cerraba la app en deep-links, tracking que duplicaba puntos, pickers de obra vacíos, tokens CSS invisibles.
- **Rollback:** `update sgc.app_versiones set publicada=(version='1.68.0') where plataforma='movil';` + `git revert 9395a21 9e10db0 c9b40ea && git push`. Las migraciones son aditivas (no requieren rollback; si se quisiera, revertir cada función a su definición previa).
- **PENDIENTE (Xaviel):** decisión QA-42 (término "pre-uso"); device-QA con dos teléfonos; follow-ups menores en §9 del informe (barrido de `navGuard.back` a las ~80 páginas restantes, repintar trazo de firma al rehidratar, `[ultimo]` en cierre de mantenimiento).

## SESIÓN 08/08 — PROMPT-14 ronda AJ (app) — Terminar Transporte v3 + nav + tracking + mensajería — **6 FASES, RELEASE 1.68.0 PUBLICADO (rolling)**
**Depende de PROMPT-13 (SGC `0f7a511`, 6 migraciones `sql/2026-08-08-aj*` en prod).** `npm run build` **VERDE (exit 0)**. **SHIPPED:** commit **`80cbcfa`** → push `main` → **PWA desplegado por Vercel**. **APK 1.68.0** firmado (cert prod `3c5316d8…5065`) + registrado (Y1, changelog estructurado por módulo `{t,d,m}`) + subido al bucket (`csd-app-1.68.0.apk` + `latest` + `version.json`, `apk_url` seteado). **`publicada=true` para 1.68.0** (1.67.0 → false) → **`version_publicada()` = 1.68.0**, `minima` = 1.42.0 (nadie bloqueado; todos los ≥1.42.0 reciben el aviso de actualizar). **Rollback:** `update sgc.app_versiones set publicada=(version='1.67.0') where plataforma='movil';` + `git revert 80cbcfa && git push` para el PWA. **Único pendiente = device-QA con DOS teléfonos** (chofer + receptor, uno Xiaomi) — físico, tuyo; ya está en manos de usuarios.

### FASE 1 — Navegación + pantalla de actualización (AJ2/AJ7/AJ3/AJ1) ✅
- **AJ7 (raíz encontrada y arreglada):** `push.service` deep-linkeaba con `router.navigateByUrl` directo → un push de "ruta asignada" (auto-ruta AF23 al emitir conduce) te sacaba del formulario. La guarda solo cubría el botón físico Atrás. **Fix estructural:** `NavGuardService.requestNav()` DIFIERE cualquier navegación de segundo plano mientras hay un formulario abierto y la descarga al cerrarlo. El deep-link de push ahora pasa por ese gate.
- **AJ2:** obra era el único módulo que usaba `router.navigate(['/obra'])` (PUSH) como "atrás" → ensuciaba el histórico y el botón físico ciclaba. Los **9 back de submódulos de obra** ahora usan `navGuard.back(fallback)` (POP real; nunca sale de la app en un deep-link de arranque en frío). `GuardedWizard.salir()` también usa el back seguro. Auditoría completa: ningún otro módulo tenía el antipatrón; RRHH (`/rrhh/empleados`) y mono-módulo auto-enter son por diseño (aterrizan en el home del módulo).
- **AJ1:** la pantalla de actualización pinta el changelog como **bullets agrupados por módulo** (`app_versiones.cambios[{t,d,m}]`, RLS legible) con chips por tipo, área scrolleable de altura limitada y botones SIEMPRE fijos abajo; fallback a texto plano por oraciones para versiones viejas.
- **AJ3:** "Falta charla" → banner claro y accionable **"Charla de seguridad de hoy: pendiente — Registrar →"** gateado a `obra.plan_dia` (oculto para el resto; ya no es residuo confuso).

### FASE 2 — Conduce por hojas + dropdowns + despachantes elegibles (AJ6/AJ13) ✅
- Crear conduce **rehecho como wizard de HOJAS** (step-bar + WizardFooter): Origen → Destino → Materiales → Foto de recepción → Despachante+Vehículo+Firmas → Resumen → Emitir. **Ningún listado abierto** (antes `collapsible-select` mostraba la lista completa sin selección → esa era la queja de la v1.67; ahora una hoja a la vez). Borrador AE9 intacto.
- **Se agregó el selector de VEHÍCULO** en la hoja de despacho (no existía en el HTML) → ahora AI6 (vehículo distinto) puede dispararse desde el conduce.
- Despachantes: la elegibilidad es server-side (PROMPT-13 `despachantes_disponibles` excluye oficina). La app recarga con contexto (`bodega`/`obra`) para que los vinculados salgan primero.

### FASE 3 — Estados del conduce + confirmación del receptor en SU teléfono (AJ8) ✅
- **Bug pendiente-invisible:** ya arreglado server-side (PROMPT-13, no filtra `es_prueba`); la app lista `mis_conduces_pendientes_entrega`.
- **Estados del chofer** (rehice `conduces/entrega`): Iniciar tránsito → Estoy entregando → **Marcar entregado (foto obligatoria, SIN firma del receptor)**. Por outbox (`conduce_estado_op`, `conduce_entregado`).
- **Receptor confirma en SU teléfono:** nueva página `/transporte/por-confirmar` (`mis_entregas_por_confirmar`) → foto + firma + ¿llegó todo? → `conduce_confirmar_receptor` (por outbox `conduce_confirmar`; server impide que confirme quien entregó). Banner de descubrimiento en el **Home** ("Tienes N entregas por confirmar") + tile en el hub de Conduces + deep-link de push. Ruta A,P-only (el receptor puede ser inventario/obra sin flota).
- **"Devolver" eliminado como submódulo** → el tile "Devolver a suplidor" abre el wizard de conduce pre-llenado (destino suplidor).

### FASE 4 — Mi actividad / perfil / rutas (AJ9/AJ10/AJ11/AJ12) ✅
- **AJ9:** filtro de periodo con **"1 semana"** (primero) + los 6 tiles ya presentes (Rutas/Conduces/Galones/KM/Inspecciones/Multas).
- **AJ11:** ficha **"Ver mi perfil de conductor"** (solo lectura, cuadrículas 2xN) en Mi actividad, vía `mi_perfil_conductor()`.
- **AJ10:** verificado — el desvío AI6 (vehículo no asignado → Uso de vehículo → vuelve al borrador) está en **crear-ruta Y conduce** (a este último le faltaba el selector de vehículo, ya agregado).
- **AJ12:** los campos de detalle de ruta (H.I/H.F/km/duración/trayecto) ya vienen de AI3; el replay visual de la polyline queda en FASE 5.

### FASE 5 — Seguimiento: tracking + trazado (AJ14) 🟡 app-side ✅ / nativo+Xiaomi PENDIENTE
- **HECHO (app):** cada punto GPS se **taggea con `ruta_id`** (habilita la consolidación server-side del trayecto). **Trazado en vivo:** el mapa de Seguimiento dibuja el **breadcrumb** (`ruta_breadcrumb_vivo`) de cada ruta activa (Google Maps y Leaflet), refrescado por realtime.
- **YA EXISTÍA (AF27/AG11):** foreground service (`@capacitor-community/background-geolocation` con notificación), watchdog que re-arma el watcher, batching + buffer offline, gate GPS, telemetría Y6. Los filtros de accuracy/saltos ahora son server-side (PROMPT-13).
- **PENDIENTE (nativo/dispositivo, TUYO):** (a) **exclusión de optimización de batería** con pantalla explicativa (crítico Xiaomi/MIUI) — requiere un plugin nativo, no se puede solo en TS; (b) muestreo adaptativo por tiempo (hoy `distanceFilter:40`); (c) **replay de la polyline consolidada** en el detalle de la ruta finalizada (los puntos `trayecto` ya se traen; falta el mini-mapa); (d) **validación con una ruta real en el Xiaomi**.

### FASE 6 — Launcher con tamaños + mensajería (AJ4/AJ5) ✅
- **AJ4:** modo edición del home ahora **redimensiona tiles** (1x1 / 2x1 / 2x2, botón ⤢ que cicla) además de reordenar; persiste en `get/set_module_order` (columna `size`). El permiso es **delegable**: `plataforma.layout_app` (ya no hardcodeado a admin) vía `puedeOperarSubmodulo`.
- **AJ5:** módulo **Mensajes** nuevo (mismo modelo que la web): `/mensajes` (conversaciones + nueva por búsqueda de usuario) y `/mensajes/:id` (hilo con envío **offline por outbox** idempotente por `client_id`, realtime Supabase, marcar leído). Tile general en el Home con **badge de no leídos** (`contar_mensajes_no_leidos`) y deep-link de push. **v1 = solo texto** (adjuntos: el contrato los soporta pero la UI de compose es texto; queda como follow-up).

### Archivos nuevos
`pages/transporte/por-confirmar/*`, `pages/mensajes/*` (+ `thread/*`), `core/services/mensajes.service.ts`.
### Servicios/áreas tocadas
`nav-guard.service` (gate + back seguro), `push.service` (requestNav), `guarded-wizard`, `version.service`+`actualizar` (changelog), obra (9 back + charla), `generar-conduce` (wizard), `conduces.service` (estados/confirmar/entregado + handlers), `conduces/entrega` (state machine), `conduces-hub` (Devolver→conduce, Por confirmar), `home` (banners receptor + Mensajes + tamaños), `mi-actividad`+`conductores.service` (semana + perfil), `tracking.service`+`seguimiento` (ruta_id + breadcrumb), `en-proceso.service`, `module-order.service`, `notificaciones.service` (deep-link /mensajes), `app.routes.ts`.

### PENDIENTE (Xaviel) — nada bloquea, ya está publicado
1. **Device-QA DOS teléfonos** (chofer + receptor, uno Xiaomi): conduce por hojas → pendiente entrega (badge real) → estados → entregado → **receptor confirma DESDE SU teléfono**; navegación sin saltos; tracking en vivo + breadcrumb en el Xiaomi; mensajería entre dos usuarios; tamaños de tiles; changelog legible. Si algo falla, rollback con el SQL de arriba.
2. FASE 5 nativo (batería/Xiaomi + replay polyline) y adjuntos en Mensajes = follow-up.
3. Criterio final de despachantes y `despachante_test_user_id` (parámetros SGC, sin código).

---

## Estado operativo (NOW)
- **Release actual: 1.75.0 PUBLICADA (rolling).** `version_publicada()` = 1.75.0 · `minima` = 1.70.0. PWA en `main` (Vercel, push hecho) + APK 1.75.0 en el bucket `app-releases`. Último commit: `cc7319f`.
- **Publicar una versión a usuarios (flip rolling):** `update sgc.app_versiones set publicada=true where plataforma='movil' and version='X'; update sgc.app_versiones set publicada=false where plataforma='movil' and version='<anterior>';`. Registrar en el historial (Y1) NO publica — publicar es un paso aparte.
- **Rollback de release:** flip `publicada` a la versión anterior + `git revert <commit> && git push` para el PWA.
- **Cert de firma del APK (prod):** SHA-256 `3c5316d8…5065` (mismo desde 1.18.0; no cambiarlo o rompe las actualizaciones).
- **Piso de compatibilidad:** WebView/Chromium **≥ 111** (Angular 21). Guard nativo en MainActivity desde 1.57.0 (memoria `webview-too-old-blank-screen`).

## Comandos
- Dev/PWA `npm start` · Build `npm run build` (debe pasar antes de "done").
- APK: `npm run apk` (build firmado + registra versión Y1) → `npm run apk:publish` (sube al bucket).
- Migraciones: `node scripts/apply-migration.mjs sql/<file>.sql` (Management API vía `SUPABASE_ACCESS_TOKEN`). Cada migración: RLS + grants de schema + grants de secuencia; RPCs retrocompatibles ≥2 versiones. Secrets en `.env.local` (gitignored).

## Pendiente — solo Xaviel (verificar si ya está hecho; no bloquea el release)
1. **Seguridad AG1:** rotar/restringir en Google Cloud Console (`csd-core`) la Google/Android API key que estuvo en `google-services.json`, cerrar la alerta de GitHub y activar Push Protection. Checklist en `SECURITY-AG1-REMEDIATION.md`. La key sigue comprometida hasta rotarla. (La historia del repo ya se purgó con BFG; la anon key de Supabase es pública por diseño.)
2. **Google Maps (opcional; hoy el Seguimiento usa Leaflet):** (a) Cloud Console `csd-core` → habilitar Maps JavaScript API; (b) crear key con *HTTP referrers* `https://app.sgcconstructorasd.com/*`, `https://localhost/*`, `http://localhost:*/*` y *API restrictions* = solo Maps JS; (c) `update sgc.parametros set valor='LA_KEY' where clave='google_maps_api_key';` (no toca el repo; se sirve por RPC `maps_api_key()`). Si la key falla, cae a Leaflet.
3. **Device-QA 1.68.0 con DOS teléfonos** (chofer + receptor, uno Xiaomi) — ver la sesión de arriba.
4. **SGC web:** marcar los vehículos a ocultar a choferes (Cantus/moto) si aún no; revertir el acceso temporal al módulo `inventario` de los choferes (sus funciones viven en Transporte; el app ya lo oculta).

## Follow-ups — Claude puede hacer (no urgentes)
- FASE 5 nativo: exclusión de optimización de batería (Xiaomi/MIUI) con pantalla explicativa (necesita plugin nativo) + muestreo adaptativo por tiempo; **replay** de la polyline consolidada en el detalle de la ruta (`ruta_trayecto`; los puntos ya se traen); migrar `location-picker` a Google Maps.
- Mensajes: adjuntos (foto) en el compose (el contrato `enviar_mensaje` ya los soporta; hoy la app manda solo texto).

## Gotchas / hechos durables
Viven en la **memoria del proyecto** (`.claude/.../memory/MEMORY.md`) — no los dupliques aquí. Los más caros: WebView <111 = pantalla en blanco; permiso de ubicación DENEGADO ≠ bug de GPS; `usuarios`/`proyectos` con RLS restrictiva → usar RPC security-definer; buckets con `upsert` necesitan política UPDATE; el conductor "TEST Conductor Prueba" está ligado a la cuenta admin (no usarlo como chofer de prueba aislado); PWA iOS no puede consultar permisos de cámara (persistir flag). Contratos server-side vigentes: PROMPT-13 (AJ) en prod (SGC `0f7a511`).

## Historial de releases (una línea; el changelog completo está en `sgc.app_versiones` y en git)
- **1.69.0** (10/08) PROMPT-15 AJ15 — auditoría QA completa (`INFORME-AUDITORIA-APP.md`) + backlog resuelto: conduce offline, nav estable (deep-links), receptor registra faltante, tracking sin duplicar puntos, pickers de obra, tokens CSS invisibles, traspaso idempotente. 3 migraciones aditivas en prod. *(detalle arriba)*
- **1.68.0** (08/08) PROMPT-14 AJ — conduce por hojas, receptor confirma en su teléfono, mensajería, launcher con tamaños, nav estable, trazado en vivo.
- **1.67.0** (07/08) PROMPT-12 AI — Transporte v3: menú 3-col, conduce simplificado (foto+despachante+2 firmas), Uso de vehículo, Aviso de vehículo, Mi actividad por periodo; fix del bucle "actualizar" en PWA; obras/almacenes como dropdown.
- **1.66.0** (07/08) PROMPT-10 AH — conduces (2 firmas al emitir, transferencia entre choferes, evidencia obligatoria), paradas completables con GPS, dropdowns AH10, RRHH (empleados+asignaciones AF33), Compras de obra.
- **1.65.0** (06/08) PROMPT-8 AG16 "Mi obra" — plan del día+charla, NC/incidentes, checklists, recursos/pedidos, subcontratistas, avance, logística, informe semanal (PDF+email).
- **1.64.0** (06/08) PROMPT-6 AG — fix del secret leak (AG1), mantenimiento (historial+cierre), robustez de tracking (watchdog/telemetría), tareas dinámicas (conduce end-to-end), Google Maps + leyenda. *(Construida pero superada por 1.65.0; de aquí vienen los pendientes AG1 y Google Maps de arriba.)*
- **1.61.0–1.63.0** (04/08) PROMPT-2/4 AF — Transporte v2 a fondo.
- **1.57.0–1.60.0** (03/08) — QA de código de Transporte + guard nativo de WebView <111.
- **1.44.0–1.55.0** (31/07–03/08) — rondas AC/AD/AE: feedback de campo de Transporte, borradores AE9, ferretería, devoluciones.
- **1.32.0–1.37.0** (28–30/07) — rondas Y/AA: Proyectos+Cronograma, bitácora, dudas/guías.
- **1.18.0–1.31.0** (20–27/07) — rondas P–Z: flota, combustible, pre-uso v2, QA en equipo real (bugs de prod cerrados), historial de versiones.
- **1.0.0–1.16.0** (07–18/07) — M1 foundations (auth+PIN, outbox offline, design system, PWA) → primeras rondas (bitácora, inventario, conduces, login conductor, fix crash de foto Android).

## Verify on resume
- `git log --oneline -4` (último = `9395a21`, bump 1.69.0; antes `9e10db0`/`c9b40ea` PROMPT-15).
- `npm run build` → exit 0.
- `select sgc.version_publicada();` → publicada 1.69.0 / minima 1.42.0.
